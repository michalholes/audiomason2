name: metadata_sync
version: 1.0.0
description: Synchronous metadata fetcher - Google Books and OpenLibrary
author: Michal Holes
entrypoint: plugin:MetadataSync

interfaces:
  - IProcessor

config:
  providers:
    - googlebooks
    - openlibrary
  timeout: 10
