# seed a deterministic latest archived failure for -l rerun-latest
FILES = [
    "badguys/tmp/rerun_latest_seed.txt",
]

# Intentionally no changes -> SCOPE:NOOP, creating an unsuccessful archive entry.
