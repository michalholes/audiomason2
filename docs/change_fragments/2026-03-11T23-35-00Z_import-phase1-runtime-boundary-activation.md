2026-03-11T23:35:00Z

Activate the import Phase 1 runtime boundary in registry bootstrap and runtime dispatch so the default import flow can resolve and execute the runtime snapshot step deterministically.
