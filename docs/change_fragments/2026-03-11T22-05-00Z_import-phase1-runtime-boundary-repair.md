2026-03-11T22:05:00Z

Removed the import phase1 runtime monkeypatch repair path, restored the runtime primitive to a pure primitive module, and stopped precomputing the runtime snapshot outside the primitive boundary.
