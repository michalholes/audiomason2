2026-03-14T22:10:00Z
Fixed scoped import Phase I selection so author-scoped and book-scoped paths derive canonical
selection domains, skip duplicate prompts when scope is already unique, and keep web prompt-select
projection fetches limited to prompt-select steps.
