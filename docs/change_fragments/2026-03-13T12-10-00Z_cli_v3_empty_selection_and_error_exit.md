2026-03-13T12:10:00Z

Import CLI v3 now preserves the default Enter path for empty discovery selections and stops immediately on submit validation errors instead of re-rendering the same prompt in a loop.
