# import cover summary grouped by book

- Updated PHASE 1 cover discovery summary formatting to emit one line per selected source/book.
- Replaced semicolon-joined flat text with multiline bullet output, preserving deterministic order.
- This makes CLI cover autodetection hints scannable for multi-book imports and archive bundles.
