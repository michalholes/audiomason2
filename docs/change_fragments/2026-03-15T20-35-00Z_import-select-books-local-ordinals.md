2026-03-15T20:35:00Z
Repaired import Phase 1 select_books ordinal mapping so filtered book prompts use the same local ordering for display, prefill, validation, and submit state sync.
Aligned import plan selection with the same source-scoped book units so Phase 1 selections keep plan summaries and batch contents consistent.
