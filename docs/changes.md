# Changes

This file lists notable changes in AudioMason2, grouped by issue.

## Issue 554

- Added new file I/O root `wizards` for wizard-owned filesystem data.
- New configuration key: `file_io.roots.wizards_dir` (legacy fallback: `wizards_dir`).
- Default location: `/tmp/audiomason/wizards`.
