MASTER_SPEC (human-readable)
================================================================================
META
--------------------------------------------------------------------------------
authoritative: True
counts: {'rules': 373, 'capabilities': 9, 'providers': 8, 'routes': 3, 'surfaces': 3, 'implementations': 3}
derivation_allowed: False
description: Clean authoritative contract model. JSONL is the single source of truth.
id: META.MASTER_SPEC_V2_0_0
version: 2.0.0-authoritative

SURFACES
--------------------------------------------------------------------------------
[SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE]
  kind: section
  source_file: docs/spec/10_architecture.md
  heading: AudioMason2 - Project Specification (Authoritative)
  route_ref: ROUTE.SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE.V1
  requires_capabilities (8):
    - CAP.SCOPE_001
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_005
    - CAP.SCOPE_006
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE]
  kind: section
  source_file: docs/spec/20_wire_contracts.md
  heading: AudioMason2 - Wire Contracts Specification (Authoritative)
  route_ref: ROUTE.SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE.V1
  requires_capabilities (5):
    - CAP.SCOPE_002
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE]
  kind: section
  source_file: docs/spec/30_implementation_bindings.md
  heading: AudioMason2 - Implementation Bindings Specification (Authoritative)
  route_ref: ROUTE.SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE.V1
  requires_capabilities (6):
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_004
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_009

ROUTES
--------------------------------------------------------------------------------
[ROUTE.SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE.V1]
  surface_id: SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE
  provider_chain (8):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.JOB_RUNNER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.ERROR_SCHEMA
    - PROV.JSON_SCHEMA
  covers_capabilities (8):
    - CAP.SCOPE_001
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_005
    - CAP.SCOPE_006
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[ROUTE.SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE.V1]
  surface_id: SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE
  provider_chain (7):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.ERROR_SCHEMA
    - PROV.JSON_SCHEMA
  covers_capabilities (5):
    - CAP.SCOPE_002
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[ROUTE.SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE.V1]
  surface_id: SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE
  provider_chain (7):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.JOB_RUNNER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.JSON_SCHEMA
  covers_capabilities (6):
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_004
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_009

PROVIDERS
--------------------------------------------------------------------------------
[PROV.CONFIG_RESOLVER]
  provides_capabilities (1):
    - CAP.SCOPE_005

[PROV.ENDPOINT_GATE]
  provides_capabilities (5):
    - CAP.SCOPE_002
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[PROV.ERROR_SCHEMA]
  provides_capabilities (1):
    - CAP.SCOPE_008

[PROV.FILE_IO]
  provides_capabilities (1):
    - CAP.SCOPE_003

[PROV.JOB_RUNNER]
  provides_capabilities (0):

[PROV.JSON_SCHEMA]
  provides_capabilities (3):
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_004

[PROV.REGISTRY]
  provides_capabilities (1):
    - CAP.SCOPE_009

[PROV.UI_EDITOR]
  provides_capabilities (2):
    - CAP.SCOPE_005
    - CAP.SCOPE_006

CAPABILITIES
--------------------------------------------------------------------------------
[CAP.SCOPE_001]
  applies_to: ARCH
  triggers_rules (34):
    - RULE.ARCH.ALL_BEHAVIOR_MUST_OBSERVABLE_VIA_LOGS
    - RULE.ARCH.ANY_IMPLEMENTATION_PATCH_REFACTOR_FEATURE_MUST
    - RULE.ARCH.CACHES_IF_IMPLEMENTED_CACHE_KEYS_MUST
    - RULE.ARCH.CONDITIONAL_BRANCHING_MUST_LIVE_WIZARD_ENGINE
    - RULE.ARCH.DEFINE_WHAT_AUDIOMASON2_MUST
    - RULE.ARCH.EACH_CHANGE_ENTRY_DOCS_CHANGES_MD
    - RULE.ARCH.ENGINE_MUST_ENFORCE_MANDATORY_CONSTRAINTS_PRESERVE
    - RULE.ARCH.EVENTS_MUST_INCLUDE_AT_MINIMUM_JOB
    - RULE.ARCH.EVERY_CHANGE_DELIVERED_PATCH_MUST_RECORDED
    - RULE.ARCH.EVERY_CHANGE_THIS_SPECIFICATION_MUST_INCREMENT
    - RULE.ARCH.FLOWMODEL_BUILD_MUST_LIGHTWEIGHT
    - RULE.ARCH.IMPORT_WIZARD_MUST_USE_SINGLE_INTERPRETER
    - RULE.ARCH.IT_MUST_CONTAIN_ONLY_ISO_8601
    - RULE.ARCH.IT_MUST_NOT_CONTAIN_ISSUE_NUMBERS
    - RULE.ARCH.JOBS_MUST_EMIT_LIFECYCLE_DIAGNOSTICS_THROUGH
    - RULE.ARCH.JOBS_MUST_PERSISTED
    - RULE.ARCH.JOBS_MUST_PERSISTED_2
    - RULE.ARCH.LONG_RUNNING_OPERATIONS_MUST_ASYNCHRONOUS
    - RULE.ARCH.MANDATORY_ORDERING_CONSTRAINTS_MUST_HOLD_SELECT
    - RULE.ARCH.MANDATORY_STEP_IDS_MUST_EXIST_EVERY
    - RULE.ARCH.MUST_RECOMPUTE_NEVER_SILENTLY_FAIL_ENABLING
    - RULE.ARCH.PARALLELISM_MUST_BOUNDED_HARD_MAX_PARALLELISM
    - RULE.ARCH.PERFORMANCE_RULES_NON_FUNCTIONAL_CONSTRAINTS_BUT
    - RULE.ARCH.PLAN_PREVIEW_BATCH_OUTPUT_MUST_BOUNDED
    - RULE.ARCH.PRODUCES_PHASE_2_JOB_REQUESTS_ONLY
    - RULE.ARCH.PROGRESS_LOGS_MUST_OBSERVABLE_WHILE_WORK
    - RULE.ARCH.RENDERERS_CLI_WEB_MUST_NOT_CONTAIN
    - RULE.ARCH.REPOSITORY_MUST_INCLUDE_DETERMINISTIC_TESTS_THAT
    - RULE.ARCH.SELECTION_PARSER_MUST_LINEAR_TIME
    - RULE.ARCH.SYNCHRONOUS_SHORTCUTS_FORBIDDEN_EXCEPT_TRIVIAL_OPERATIONS
    - RULE.ARCH.SYSTEM_MUST_REMAIN_FUNCTIONAL_IF_IMPORT
    - RULE.ARCH.THEY_MUST_NOT_IMPLEMENT_VALIDATION_TRANSITIONS
    - RULE.ARCH.UI_LAYERS_CLI_WEB_MUST
    - RULE.ARCH.UI_MUST_NEVER_BLOCK_PROCESSING

[CAP.SCOPE_002]
  applies_to: ARTIFACT | ENDPOINT
  triggers_rules (57):
    - RULE.ARCH.ANY_ATTEMPT_INTERACT_USER_DURING_PHASE
    - RULE.ARCH.CALLABLES_MUST_ACCEPT_SINGLE_ARGUMENT_ARGV
    - RULE.ARCH.CLI_HOST_MUST_NOT_CALL_GET
    - RULE.ARCH.CLI_HOST_MUST_TRANSPARENTLY_SUPPORT_BOTH
    - RULE.ARCH.COMMAND_NAMES_MUST
    - RULE.ARCH.DICTIONARY_KEYS_MUST_CLI_COMMAND_NAMES
    - RULE.ARCH.DICTIONARY_VALUES_MUST_CALLABLES
    - RULE.ARCH.IMPLICIT_HEURISTIC_DETECTION_FORBIDDEN
    - RULE.ARCH.PLUGINS_THAT_DECLARE_ICLICOMMANDS_INTERFACE_MUST
    - RULE.ARCH.PLUGINS_THAT_DO_NOT_DECLARE_THIS
    - RULE.ARCH.PLUGINS_THAT_PROVIDE_CLI_COMMANDS_MUST
    - RULE.ARCH.PLUGIN_CLASS_MUST_EXPOSE_METHOD
    - RULE.LAYOUT.ACTIONS_CONTRACT_NORMATIVE_ACTIONS_MUST_DERIVED
    - RULE.LAYOUT.ACTION_STEPS_EXECUTION_JOB_INTERPRETER_MUST
    - RULE.LAYOUT.ALL_FILE_REFERENCES_MUST_USE_ROOT
    - RULE.LAYOUT.ALL_JOB_REQUEST_SERIALIZATION_MUST_CANONICAL
    - RULE.LAYOUT.ANY_DEVIATION_FROM_CANONICAL_JOB_REQUEST
    - RULE.LAYOUT.CONFLICT_SCAN_INPUTS_NORMATIVE_CONFLICT_SCAN
    - RULE.LAYOUT.DISCOVERY_CONFLICT_ITEMS_MUST_DETERMINISTICALLY_SORTED
    - RULE.LAYOUT.EACH_CREATED_JOB_MUST_HAVE_DETERMINISTIC
    - RULE.LAYOUT.EACH_ENTRY_MUST_CONFORM_EXACTLY_CANONICAL
    - RULE.LAYOUT.EMPTY_PLAN_SOURCE_RELATIVE_PATH_NON
    - RULE.LAYOUT.FORBIDDEN
    - RULE.LAYOUT.IF_CONFLICTS_APPEAR_ENGINE_MUST_BLOCK
    - RULE.LAYOUT.JOBS_REGISTRY_UPDATES_MUST_REMAIN_SAFE
    - RULE.LAYOUT.JOB_ID_GENERATION_MUST_FOLLOW_SAME
    - RULE.LAYOUT.JOB_REQUESTS_JSON_MUST_CONTAIN_CONFIG
    - RULE.LAYOUT.JSON_STRUCTURE_MUST_MATCH_SAME_SCHEMA
    - RULE.LAYOUT.PREVENTED_DUPLICATE_START_PROCESSING_CALLS_MUST
    - RULE.LAYOUT.REQUIRED_KEYS_MUST_NOT_OMITTED
    - RULE.LAYOUT.RULES_DUPLICATE_JOB_CREATION_SAME_IDEMPOTENCY
    - RULE.LAYOUT.SESSIONS_SESSION_ID_HTML_PLAN_JSON
    - RULE.LAYOUT.SHARED_CANONICAL_SERIALIZE_UTILITY_MUST_EXIST
    - RULE.LAYOUT.START_PROCESSING_RESPONSE_CONTRACT_NORMATIVE_START
    - RULE.WIRE.10_6_ENGINE_GUARDS_INVARIANTS_MUST
    - RULE.WIRE.BACKEND_MUST_EXPOSE_GET_API_ROOTS
    - RULE.WIRE.BUNDLE_MUST_DETERMINISTIC_NO_TIMESTAMPED_FILENAMES
    - RULE.WIRE.CLI_MUST_EMIT_EXACTLY_ONE_SHUTDOWN
    - RULE.WIRE.EMBEDDING_GENERAL_PURPOSE_EXPRESSION_ENGINES_FORBIDDEN
    - RULE.WIRE.ENGINE_MUST_REJECT_ANY_WIZARDDEFINITION_FLOWCONFIG
    - RULE.WIRE.EXPRESSION_RESOLUTION_MUST_SIDE_EFFECT_FREE
    - RULE.WIRE.FINALIZED_SESSIONS_MUST_IMMUTABLE
    - RULE.WIRE.INTERPRETER_MUST_GUARANTEE
    - RULE.WIRE.INTERPRETER_MUST_RETURN_FOLLOWING_CANONICAL_STRUCTURE
    - RULE.WIRE.INVALID_REFERENCE_MUST_RAISE_VALIDATION_ERROR
    - RULE.WIRE.LITERAL_LINE_INTERRUPTED_MUST_NOT_PRINTED
    - RULE.WIRE.MUST_EMIT_PLUGIN_LOAD_FAILED_DIAGNOSTICS
    - RULE.WIRE.MUTATION_AFTER_FINALIZED_MUST_REJECTED
    - RULE.WIRE.PATH_TRAVERSAL_MUST_REJECTED_NO_SEGMENTS
    - RULE.WIRE.PREVIEW_ARTIFACTS_MUST_ISOLATED_DISPOSABLE
    - RULE.WIRE.REJECTION_MUST_USE_INVARIANT_VIOLATION_10
    - RULE.WIRE.RENDERERS_MUST_RENDER_STRICTLY_FROM_THIS
    - RULE.WIRE.REPOSITORY_MUST_ENFORCE_AUTOMATED_CHECKS_ENSURING
    - RULE.WIRE.RESPONSE_MUST_INCLUDE_ONLY_USER_FACING
    - RULE.WIRE.WEB_PLUGIN_SELECTION_MUST_DETERMINISTIC_MANIFEST
    - RULE.WIRE.WEB_UI_MUST_REPLACEABLE_WITHOUT_TOUCHING
    - RULE.WIRE.WHEN_STARTING_WEB_MODE_CLI_MUST

[CAP.SCOPE_003]
  applies_to: ARTIFACT | FILE_IO
  triggers_rules (37):
    - RULE.ARCH.ARCHIVE_CAPABILITY_MUST_ABLE_EMIT_STRUCTURED
    - RULE.ARCH.CAPABILITY_MUST_PROVIDE_AT_MINIMUM
    - RULE.ARCH.CHECKSUM_MUST_DETERMINISTIC_SHA256_OVER_FILE
    - RULE.ARCH.CORE_MUST_NOT_IMPLEMENT_STORAGE_BACKEND
    - RULE.ARCH.ENGINE_MUST_PERFORM_CONFLICT_SCAN
    - RULE.ARCH.FILE_I_O_CAPABILITY_MUST_NOT
    - RULE.ARCH.FILE_I_O_CAPABILITY_MUST_NOT_2
    - RULE.ARCH.FILE_I_O_CAPABILITY_MUST_SUPPORT
    - RULE.ARCH.FILE_I_O_PLUGIN_MUST_PROVIDE
    - RULE.ARCH.FILE_SERVICE_ITSELF_MUST_NOT_GENERATE
    - RULE.ARCH.HIGHER_LAYER_MUST_ABLE_CHOOSE
    - RULE.ARCH.HIGHER_LAYER_MUST_ABLE_SPECIFY_DESTINATION
    - RULE.ARCH.HIGHER_LAYER_MUST_ABLE_SPECIFY_EXACT
    - RULE.ARCH.LIST_DIR_ORDERING_MUST_STABLE_DETERMINISTIC
    - RULE.ARCH.RAR_UNPACK_REQUIRED_PACK_BEST_EFFORT
    - RULE.ARCH.REQUIRED_BEHAVIOR
    - RULE.ARCH.STATE_MUST_REMAIN_FINAL_SUMMARY_CONFIRM
    - RULE.ARCH.THIS_CAPABILITY_MUST_SUPPORT
    - RULE.ARCH.UI_LAYERS_CLI_WEB_TUI_MUST
    - RULE.ARCH.WHEN_FLATTENING_HIGHER_LAYER_MUST_ABLE
    - RULE.LAYOUT.10_11_JOB_REQUESTS_JSON_MUST
    - RULE.LAYOUT.10_9_ALL_WRITES_MUST_ATOMIC
    - RULE.LAYOUT.ACTION_JOBS_JSON_MUST_NOT_USED
    - RULE.LAYOUT.ALL_IMPORT_WIZARD_DATA_MUST_STORED
    - RULE.LAYOUT.BOOTSTRAP_RULES_CREATION_MUST_ATOMIC_WRITE
    - RULE.LAYOUT.CONTRACT_EACH_ENTRY_MUST_CANONICAL_JSON
    - RULE.LAYOUT.CREATION_TIMING_PLAN_JSON_MUST_CREATED
    - RULE.LAYOUT.DO_NOT_EXIST_UNDER_FILE_IO
    - RULE.LAYOUT.EFFECTIVE_MODEL_JSON_MUST_INCLUDE_ITEMS
    - RULE.LAYOUT.EXISTING_FILES_MUST_NOT_OVERWRITTEN_BOOTSTRAPPED
    - RULE.LAYOUT.FLOWCONFIG_UI_LAYERS_MUST_NOT_INTERPRET
    - RULE.LAYOUT.IF_PRESENT_THESE_ARTIFACTS_MUST_WRITTEN
    - RULE.LAYOUT.MODEL_LOAD_ABSENCE_MODELS_MUST_NOT
    - RULE.LAYOUT.MUST_NOT_MODIFY_ANY_SESSION_SNAPSHOT
    - RULE.LAYOUT.MUST_PASS_FULL_MODEL_VALIDATION_BOOTSTRAP
    - RULE.LAYOUT.REQUIRED_SUBPATHS
    - RULE.LAYOUT.SESSIONS_SESSION_ID_HTML_EFFECTIVE_MODEL

[CAP.SCOPE_004]
  applies_to: ARTIFACT | LAYOUT
  triggers_rules (6):
    - RULE.LAYOUT.ACTIVE_SESSIONS_MUST_USE_THEIR_SNAPSHOT
    - RULE.LAYOUT.AT_SESSION_START_ENGINE_MUST_CREATE
    - RULE.LAYOUT.CONFIGURATION_CHANGES_MUST_AFFECT_ONLY_NEW
    - RULE.LAYOUT.FINALIZE_MUST_PRODUCE_BYTE_IDENTICAL_CANONICAL
    - RULE.LAYOUT.MANDATORY_CRASH_RECOVERY_REQUIRED_CLI_MODE
    - RULE.LAYOUT.OVER_CANONICAL_EFFECTIVE_MODEL_JSON_MODEL

[CAP.SCOPE_005]
  applies_to: CONFIG | ENDPOINT | UI
  triggers_rules (84):
    - RULE.ARCH.ACTIVE_SESSIONS_MUST_USE_FROZEN_EFFECTIVE
    - RULE.ARCH.ALL_EDITOR_MANAGED_ARTIFACTS_MUST_IMPLEMENT
    - RULE.ARCH.ALWAYS_WINS_CONSUMERS_MUST_NOT_AWARE
    - RULE.ARCH.AT_SESSION_CREATION_ENGINE_MUST_GENERATE
    - RULE.ARCH.AUDIOMASON_IMPORT_EDITOR_MUST_REMAIN_SUPPORTED
    - RULE.ARCH.AUDIOMASON_IMPORT_MUST_NOT_EMIT_HIGH
    - RULE.ARCH.BEHAVIOR_MUST_NOT_START_RENDERER
    - RULE.ARCH.BEHAVIOR_SETTINGS_MUST_STORED_FLOWCONFIG_SEE
    - RULE.ARCH.BOOL_MUST_BOOL_NO_IMPLICIT_STRING
    - RULE.ARCH.CLI_INPUT_RULES_CLI_MUST_SET
    - RULE.ARCH.CONFIGSERVICE_MUST_RE_INITIALIZE_RESOLVER_AFTER
    - RULE.ARCH.CONFIGSOURCE_MUST_INCLUDE_SOURCE_LEVEL_NAME
    - RULE.ARCH.CONFIGURATION_IT_MUST_USE_CONFIGSERVICE
    - RULE.ARCH.CONFIGURATION_RESOLVER_MUST_PROVIDE_CANONICAL_STRUCTURED
    - RULE.ARCH.DEFAULTS_DISABLED_AUDIOMASON_IMPORT_MUST_PRINT
    - RULE.ARCH.DEFAULT_WORKFLOW_PROVIDED_SYSTEM_SHALL_REMAIN
    - RULE.ARCH.DEPRECATED_PLUGINS_SYSLOG_FILENAME_MUST_NOT
    - RULE.ARCH.EDGE_MAY_CONTAIN_CONDITION_OBJECT_IF
    - RULE.ARCH.ENGINE_MUST_NORMALIZE_V1_V2_AS
    - RULE.ARCH.ENGINE_MUST_TRANSITION_BACK_SELECT_BOOKS
    - RULE.ARCH.ENGINE_MUST_TREAT_STRUCTURE_WIZARDDEFINITION_BEHAVIOR
    - RULE.ARCH.ENGINE_SIDE_INVARIANTS_MUST_ENFORCED_DURING
    - RULE.ARCH.ENUM_MUST_ONE_ALLOWED_ENUM_VALUES
    - RULE.ARCH.ERR_PAYLOAD_STATE_MUST_NOT_ADVANCE
    - RULE.ARCH.EVALUATION_MUST_TOTAL_DETERMINISTIC_NO_EXCEPTIONS
    - RULE.ARCH.EVERY_EDGE_FROM_EDGE_MUST_REFERENCE
    - RULE.ARCH.FILESYSTEM_LISTING_PERFORMED_RENDERER_MUST_DONE
    - RULE.ARCH.FILE_I_O_CAPABILITY_ITS_ROOTS
    - RULE.ARCH.FLOWCONFIG_MUST_NOT_MODIFY_STRUCTURE
    - RULE.ARCH.IF_IMPLEMENTED_MUST_NOT_UPDATE_IMPLICITLY
    - RULE.ARCH.IMPORT_WIZARD_FLOW_MUST_REPRESENTABLE_AS
    - RULE.ARCH.IMPORT_WIZARD_STRUCTURAL_WORKFLOW_MUST_DEFINED
    - RULE.ARCH.INT_MUST_INT_NO_IMPLICIT_STRING
    - RULE.ARCH.KNOWN_KEYS_PRESENT_SCHEMA_MUST_SUPPORT
    - RULE.ARCH.LOGGING_LEVEL_CLI_MUST_NOT_SET
    - RULE.ARCH.MAY_UPDATE_DEFAULTS_ANY_AUTO_SAVE
    - RULE.ARCH.MUST_EVALUATED_ENGINE_RENDERER_NEUTRAL
    - RULE.ARCH.NODES_STEP_ID_MUST_UNIQUE
    - RULE.ARCH.NORMALIZATION_RULES_VALUE_MUST_STRING_TRIM
    - RULE.ARCH.OK_PAYLOAD_STATE_MUST_ADVANCE_DETERMINISTICALLY
    - RULE.ARCH.PATH_MUST_START_INPUTS_STATE_READ
    - RULE.ARCH.PERSISTED_ARTIFACTS_MUST_NOT_INCLUDE_TIMESTAMPS
    - RULE.ARCH.PERSISTED_WIZARDDEFINITION_MUST_NOT_INCLUDE_TIMESTAMPS
    - RULE.ARCH.PLUGINS_IMPORT_CLI_NONINTERACTIVE_IF_TRUE
    - RULE.ARCH.PLUGIN_MUST_VALIDATE_THAT_LOGGING_SYSTEM
    - RULE.ARCH.PROCESSING_MUST_REMAIN_TERMINAL_STEP
    - RULE.ARCH.PROMPT_MUST_FAIL_IF_REQUIRED_INPUTS
    - RULE.ARCH.REPORTED_STDERR_SYSTEM_MUST_NOT_CRASH
    - RULE.ARCH.RESOLVER_CONFIG_HARD_CODED_DEFAULTS_LAUNCHER
    - RULE.ARCH.RESOLVER_MUST_PROVIDE_DETERMINISTIC_LIST_KNOWN
    - RULE.ARCH.RESOLVER_VALUES_CURRENT_RUN_PRECEDENCE_MUST
    - RULE.ARCH.RESOLVE_ALL_MUST_ENUMERATE_AT_LEAST
    - RULE.ARCH.ROOTS_PATH_MUST_RELATIVE_PATH_MUST
    - RULE.ARCH.ROOT_PATH_RULES_ROOT_MUST_VALIDATED
    - RULE.ARCH.SELECT_AUTHORS_MUST_REMAIN_ENTRY_STEP
    - RULE.ARCH.SOURCE_EACH_RESOLVED_VALUE_MUST_TRACEABLE
    - RULE.ARCH.SPECIAL_RULE_PLAN_PREVIEW_BATCH_ERROR
    - RULE.ARCH.STEP_BEHAVIOR_SETTINGS_EDITS_MUST_ONLY
    - RULE.ARCH.STRUCTURAL_EDITS_ADD_REMOVE_REORDER_STEPS
    - RULE.ARCH.SUBSCRIBERS_PLUGINS_THAT_SUBSCRIBE_LOGBUS_PERSISTENCE
    - RULE.ARCH.SUCH_LOGS_MUST_REMAIN_AVAILABLE_VIA
    - RULE.ARCH.TERMINAL_NODE_PROCESSING_PHASE_1_MUST
    - RULE.ARCH.UI_LAYERS_MUST_NOT_EDIT_RAW
    - RULE.ARCH.UI_MUST_MAINTAIN_TWO_INDEPENDENT_DRAFTS
    - RULE.ARCH.UI_ONLY_IT_MUST_DELEGATE_VALIDATION
    - RULE.ARCH.UNKNOWN_KEYS_MUST_NOT_MAKE_RESOLVER
    - RULE.ARCH.VALIDATE_ALL_UI_MUST_VALIDATE_BOTH
    - RULE.ARCH.VALUE_LIST_VALUES_MUST_JSON_PRIMITIVES
    - RULE.ARCH.WARNINGS_ERRORS_MUST_REMAIN_VISIBLE_NORMAL
    - RULE.ARCH.WIZARDDEFINITION_MUST_VERSIONED_VALIDATED_LOAD
    - RULE.ARCH.WIZARD_MUST_FOLLOW_GLOBAL_THREE_PHASE
    - RULE.LAYOUT.INDEX_JSON_MUST_CONTAIN_UNIQUE_LIST
    - RULE.LAYOUT.MUST_STORED_UNDER_FLOW_CONFIG_DEFAULTS
    - RULE.LAYOUT.PERSISTED_ARTIFACTS_MUST_NOT_INCLUDE_TIMESTAMPS
    - RULE.LAYOUT.THIS_SEPARATION_NORMATIVE_STRUCTURE_BEHAVIOR_RENDERERS
    - RULE.WIRE.DEBUG_MODE_HTTP_REQUESTS_THAT_RECEIVE
    - RULE.WIRE.DEBUG_MODE_NON_2XX_HTTP_RESPONSES
    - RULE.WIRE.ERRORS_FROM_CONFIG_SET_UNSET_MUST
    - RULE.WIRE.ERROR_CAPTURE_MUST_FAIL_SAFE_MUST
    - RULE.WIRE.LOGS_UI_MUST_EXPOSE_DOWNLOAD_DEBUG
    - RULE.WIRE.LOGS_UI_MUST_INCLUDE_CLIENT_SIDE
    - RULE.WIRE.WEB_INTERFACE_EXPOSES_UI_ONLY_CONFIGURATION
    - RULE.WIRE.WEB_SERVER_MUST_NOT_TAIL_WEB
    - RULE.WIRE.WHEN_WEB_INTERFACE_DEBUG_ENABLED_WEB

[CAP.SCOPE_006]
  applies_to: CORE | PLUGIN | UI
  triggers_rules (26):
    - RULE.ARCH.ANONYMOUS_HIDDEN_COMMAND_REGISTRATION_FORBIDDEN
    - RULE.ARCH.ANY_COLLISION_MUST_RESULT_DETERMINISTIC_ERROR
    - RULE.ARCH.AT_CLI_STARTUP_SYSTEM_MUST_PERFORM
    - RULE.ARCH.CLI_COMMAND_REGISTRATION_MUST_RESPECT_PLUGINREGISTRY
    - RULE.ARCH.CLI_HELP_OUTPUT_MUST_DISTINGUISH_CORE
    - RULE.ARCH.COMMAND_NAME_COLLISIONS_FORBIDDEN
    - RULE.ARCH.CORE_MUST_NEVER_IMPLEMENT_AUDIO_PROCESSING
    - RULE.ARCH.CORE_MUST_NOT_PROVIDE_GLOBAL_CLI
    - RULE.ARCH.CORE_MUST_REMAIN_SMALL_READABLE_STABLE
    - RULE.ARCH.DISABLED_PLUGINS_MUST_NOT_REGISTER_CLI
    - RULE.ARCH.FULL_PLUGIN_IMPORT_INITIALIZATION_MUST_NOT
    - RULE.ARCH.HELP_OUTPUT_MUST_INDICATE_PROVIDING_PLUGIN
    - RULE.ARCH.HYBRID_LOADING_MODEL_REQUIRED
    - RULE.ARCH.INSTALLATION_MECHANISM_MUST_ABSTRACTED
    - RULE.ARCH.MANIFEST_ONLY_DISCOVERY_MUST_NOT_EXECUTE
    - RULE.ARCH.MUST_NOT_MODIFY_OVERRIDE_INTERCEPT_DISABLE
    - RULE.ARCH.MUST_STILL_APPEAR_HELP_OUTPUT_MUST
    - RULE.ARCH.ORDERING_HELP_OUTPUT_MUST_DETERMINISTIC_STABLE
    - RULE.ARCH.ORIGIN_EVERY_PLUGIN_PROVIDED_CLI_COMMAND
    - RULE.ARCH.PLUGINS_MUST_ISOLATABLE_REMOVABLE_WITHOUT_BREAKING
    - RULE.ARCH.PLUGINS_MUST_NOT_ASSUME_FILESYSTEM_LAYOUT
    - RULE.ARCH.PLUGINS_MUST_NOT_OVERRIDE_SHADOW_CORE
    - RULE.ARCH.PLUGIN_FAILURE_MUST_NOT_CRASH_SYSTEM
    - RULE.ARCH.PLUGIN_PROVIDED_CLI_COMMAND_NAMES_MUST
    - RULE.ARCH.RESERVED_CORE_COMMAND_NAMES_PLUGINS_MUST
    - RULE.ARCH.RUNTIME_MUTATION_REPOSITORY_PLUGINS_FORBIDDEN

[CAP.SCOPE_007]
  applies_to: ENDPOINT
  triggers_rules (62):
    - RULE.ARCH.ALL_CLI_PLUGIN_COMMAND_BEHAVIOR_MUST
    - RULE.ARCH.BUILT_PLUGIN_LOADING_MUST_WORK_WITHOUT
    - RULE.ARCH.FILESYSTEM_ORDER_MUST_NOT_AFFECT_BEHAVIOR
    - RULE.ARCH.PACKAGE_CORE_PLUGIN_LOADER_MUST_ENSURE
    - RULE.ARCH.PLUGIN_PROCESSING_ORDER_MUST_EXPLICITLY_DEFINED
    - RULE.ARCH.USER_SYSTEM_PLUGIN_DIRECTORIES_MUST_NOT
    - RULE.LAYOUT.GET_IMPORT_UI_STEPS_INDEX_ROUTE
    - RULE.LAYOUT.IF_STEP_HAS_NO_HUMAN_FRIENDLY
    - RULE.LAYOUT.IMPORT_PLUGIN_MUST_PROVIDE_SINGLE_INTERNAL
    - RULE.LAYOUT.STEPCATALOG_METADATA_READ_ONLY_MUST_DETERMINISTIC
    - RULE.WIRE.ALL_EDITOR_HISTORY_ROLLBACK_BEHAVIOR_MUST
    - RULE.WIRE.ALL_MODES_ROUTE_LEVEL_VISIBILITY_MUST
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_2
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_3
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_4
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_5
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_6
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_7
    - RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_8
    - RULE.WIRE.CONFIG_REQUIRED
    - RULE.WIRE.CONFIG_REQUIRED_2
    - RULE.WIRE.DEFAULTS_TEMPLATE_MUST_SUITABLE_AS_FLOWCONFIG
    - RULE.WIRE.DEFINITION_REQUIRED
    - RULE.WIRE.DEFINITION_REQUIRED_2
    - RULE.WIRE.DEFINITION_REQUIRED_3
    - RULE.WIRE.DESCRIPTION_MUST_ASCII_ONLY
    - RULE.WIRE.ENGINE_INVARIANT_VIOLATIONS_MUST_RETURN_INVARIANT
    - RULE.WIRE.ENGINE_INVARIANT_VIOLATIONS_MUST_RETURN_INVARIANT_2
    - RULE.WIRE.HOST_E_G_WEB_INTERFACE_MUST
    - RULE.WIRE.ID_REQUIRED
    - RULE.WIRE.ID_REQUIRED_2
    - RULE.WIRE.IF_ID_NOT_FOUND_MUST_RETURN
    - RULE.WIRE.IF_ID_NOT_FOUND_MUST_RETURN_2
    - RULE.WIRE.ITEMS_MUST_RETURNED_DETERMINISTIC_ORDER
    - RULE.WIRE.KIND_MUST_MATCH_IMPORT_PLUGIN_S
    - RULE.WIRE.MODE_MUST_ONE_STAGE_INPLACE
    - RULE.WIRE.MUST_ENFORCE_DETERMINISTIC_CONFLICT_RE_CHECK
    - RULE.WIRE.OPAQUE_CONSUMER_ROUTE_MUST_NOT_RE
    - RULE.WIRE.OUTPUT_MUST_EXACTLY_2_LINES_NOTHING
    - RULE.WIRE.PINNED_MUST_FIRST_ONLY_REQUIRED_FIRST
    - RULE.WIRE.PLUGIN_MUST_NOT_RELY_HOST_SPECIFIC
    - RULE.WIRE.QUIET_MODE_UVICORN_LOGGING_MUST_SILENCED
    - RULE.WIRE.REQUIRED_FALSE
    - RULE.WIRE.REQUIRED_FALSE_2
    - RULE.WIRE.RESPONSE_OBJECT_KEYS_MUST_EMITTED_DETERMINISTIC
    - RULE.WIRE.ROOT_PATH_MODE_REQUIRED_NO_IMPLICIT
    - RULE.WIRE.SETTINGS_SCHEMA_FIELDS_MUST_DETERMINISTIC_ORDER
    - RULE.WIRE.SETTINGS_SCHEMA_UI_ONLY_METADATA_READ
    - RULE.WIRE.STARTUP_SHUTDOWN_ACCESS_OUTPUT_UVICORN_LOG
    - RULE.WIRE.STEP_ID_MUST_ASCII
    - RULE.WIRE.STEP_ID_PATH_PARAMETER_MUST_ASCII
    - RULE.WIRE.TITLE_MUST_ASCII_IF_NO_HUMAN
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_2
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_3
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_4
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_5
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_6
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_7
    - RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_8
    - RULE.WIRE.WIZARDDEFINITION_VALIDATE_SAVE_MUST_ACCEPT_WIZARDDEFINITION

[CAP.SCOPE_008]
  applies_to: ENDPOINT | ERROR
  triggers_rules (24):
    - RULE.ARCH.DIAGNOSTICS_MUST_INCLUDE_AT_MINIMUM_SESSION
    - RULE.ARCH.FAILURE_CLI_COMMAND_PLUGIN_MUST_NOT
    - RULE.ARCH.FAILURE_MUST_NOT_CRASH_CLI_PROCESS
    - RULE.ARCH.FAILURE_ONE_PLUGIN_MUST_NOT_AFFECT
    - RULE.ARCH.IMPORT_ENGINE_MUST_EMIT_VIA_CORE
    - RULE.ARCH.OTHER_CLI_COMMANDS_MUST_REMAIN_AVAILABLE
    - RULE.ARCH.OTHER_CLI_COMMANDS_MUST_REMAIN_FUNCTIONAL
    - RULE.ARCH.PLUGIN_MUST_MARKED_AS_FAILED_CURRENT
    - RULE.ARCH.PLUGIN_THAT_FAILS_DURING_LOADING_EXECUTION
    - RULE.ARCH.SUBSEQUENT_INVOCATIONS_MUST_REJECTED_CLEAR_ERROR
    - RULE.WIRE.ALL_VALIDATION_INVARIANT_FAILURES_MUST_RETURN
    - RULE.WIRE.EACH_ITEM_MUST_INCLUDE
    - RULE.WIRE.ERR_SUBMISSIONS_MUST_NOT_MUTATE_ANSWERS
    - RULE.WIRE.FIELD_DEFINITIONS_MUST_DECLARE_REQUIRED_PROPERTIES
    - RULE.WIRE.FLOWCONFIG_EDITABLE_CONFIGURATION_OPTIONAL_STEPS_DEFAULTS
    - RULE.WIRE.FLOWCONFIG_MUST_NOT_CHANGE
    - RULE.WIRE.FLOWCONFIG_MUST_NOT_INCLUDE_TOP_LEVEL
    - RULE.WIRE.MULTI_SELECT_INDEXED_FIELDS_ENGINE_MUST
    - RULE.WIRE.OUT_RANGE_INDICES_MUST_YIELD_VALIDATION
    - RULE.WIRE.RENDERERS_MUST_TREAT_FLOWMODEL_AS_SINGLE
    - RULE.WIRE.REQUIRED
    - RULE.WIRE.RESULTING_ID_LIST_MUST_PRESERVE_ORIGINAL
    - RULE.WIRE.SELECT_AUTHORS_ITEMS_MUST_DERIVED_DETERMINISTICALLY
    - RULE.WIRE.SELECT_BOOKS_ITEMS_MUST_DERIVED_DETERMINISTICALLY

[CAP.SCOPE_009]
  applies_to: ENDPOINT | REGISTRY
  triggers_rules (43):
    - RULE.ARCH.1_PLUGIN_DISCOVERY_DIRECTORIES_MUST_PROCESSED
    - RULE.ARCH.ALL_CLI_COMMAND_PLUGIN_DISCOVERY_MUST
    - RULE.ARCH.AT_CLI_STARTUP_ONLY_STUBS_MAY
    - RULE.ARCH.CLI_HOST_MUST_PRESERVE_THIS_SOURCE
    - RULE.ARCH.CLI_MUST_ABORT_STARTUP_CLEAR_ERROR
    - RULE.ARCH.CLI_PLUGIN_MUST_MAINTAIN_INTERNAL_REGISTRY
    - RULE.ARCH.COMMAND_INVOCATION_CLI_MUST_LOAD_EXACTLY
    - RULE.ARCH.COMMAND_STUB_MUST_CONTAIN_COMMAND_NAME
    - RULE.ARCH.CONFIGRESOLVER_MUST_EXPOSE_AUTHORITATIVE_SCHEMA_REGISTRY
    - RULE.ARCH.DISCOVERED_PLUGIN_DIRECTORIES_CLI_HOST_MUST
    - RULE.ARCH.DISCOVERY_MUST_DETERMINISTIC_INDEPENDENT_FILESYSTEM
    - RULE.ARCH.EACH_SCHEMA_ENTRY_MUST_DEFINE_AT
    - RULE.ARCH.FILESYSTEM_ITERATION_ORDER_MUST_NOT_INFLUENCE
    - RULE.ARCH.IF_ICLICOMMANDS_NOT_DECLARED_CLI_COMMANDS
    - RULE.ARCH.MULTIPLE_PARALLEL_PLUGIN_STATE_MECHANISMS_FORBIDDEN
    - RULE.ARCH.OBSOLETE_UNSUPPORTED_MUST_NOT_USED_PLUGIN
    - RULE.ARCH.PHASE_3_IMPLEMENTATION_REQUIREMENT_BUILT_CLI
    - RULE.ARCH.PLUGINS_MUST_ORDERED_MANIFEST_NAME_LEXICOGRAPHICALLY
    - RULE.ARCH.PLUGIN_CONFIGURATION_HOST_CONFIGURATION_MUST_STORED
    - RULE.ARCH.PLUGIN_DIRECTORIES_MUST_SORTED_LEXICOGRAPHICALLY_DIRECTORY
    - RULE.ARCH.PLUGIN_YAML_MUST_DECLARE_COMMAND_NAMES
    - RULE.ARCH.SORTING_KEY_MUST_DIRECTORY_NAME_PATH
    - RULE.ARCH.THERE_MUST_SINGLE_PLUGINREGISTRY_API
    - RULE.ARCH.THIS_LIST_MUST_USED_BUILD_STUB
    - RULE.LAYOUT.BUNDLE_CLASSIFICATION_RULES_KIND_MUST_BUNDLE
    - RULE.LAYOUT.DISCOVERY_FINGERPRINT_MUST_EQUAL_SHA_256
    - RULE.LAYOUT.DISCOVERY_MUST_PRODUCE_CANONICAL_DISCOVERY_INPUT
    - RULE.LAYOUT.EACH_ITEM_MUST_CONTAIN_ITEM_ID
    - RULE.LAYOUT.FINISHES_SUCCESS_FAILED_JOBS_MUST_NOT
    - RULE.LAYOUT.ITEM_ID_MUST_EQUAL_ROOT_ROOT
    - RULE.LAYOUT.PROCESSED_REGISTRY_MUST_UPDATED_ONLY_WHEN
    - RULE.LAYOUT.RAR_EXTENSION_MATCHING_MUST_CASE_INSENSITIVE
    - RULE.LAYOUT.RELATIVE_PATH_MUST_USE_SEPARATORS_CONTAIN
    - RULE.WIRE.CALLABLE_OPERATIONS_MUST_DISCOVERED_EXCLUSIVELY_VIA
    - RULE.WIRE.EACH_CALLABLE_PLUGIN_MUST_PROVIDE_MANIFEST
    - RULE.WIRE.FILESYSTEM_SCANNING_FORBIDDEN
    - RULE.WIRE.INTERPRETER_MUST_ENFORCE
    - RULE.WIRE.INTERPRETER_MUST_NOT_IMPLEMENT_PARALLEL_JOB
    - RULE.WIRE.INTERPRETER_SHALL
    - RULE.WIRE.JOB_MUST_GENERATE_PHASE_1_ACTION
    - RULE.WIRE.PLUGIN_MAY_CHOOSE_STORE_BOTH_CONTRACTS
    - RULE.WIRE.REGISTRY_METADATA_MUST_INCLUDE
    - RULE.WIRE.WIZARD_CALLABLE_OPERATIONS_THROUGH_WIZARD_CALLABLE

RULES
--------------------------------------------------------------------------------
[RULE.ARCH.1_PLUGIN_DISCOVERY_DIRECTORIES_MUST_PROCESSED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L760
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    1.  Plugin discovery directories MUST be processed in this order:

[RULE.ARCH.ACTIVE_SESSIONS_MUST_USE_FROZEN_EFFECTIVE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1147
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.16 Structural Workflow Authority (WizardDefinition Model)
  statement:
    Active sessions MUST use a frozen effective_workflow snapshot.

[RULE.ARCH.ALL_BEHAVIOR_MUST_OBSERVABLE_VIA_LOGS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L129
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.3 Deterministic Behavior
  statement:
    -   All behavior must be observable via logs and job state.

[RULE.ARCH.ALL_CLI_COMMAND_PLUGIN_DISCOVERY_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L756
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    All CLI command plugin discovery MUST be deterministic.

[RULE.ARCH.ALL_CLI_PLUGIN_COMMAND_BEHAVIOR_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L723
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.9 Determinism Requirements
  statement:
    All CLI plugin command behavior MUST be deterministic.

[RULE.ARCH.ALL_EDITOR_MANAGED_ARTIFACTS_MUST_IMPLEMENT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1048
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    All editor-managed artifacts MUST implement a deterministic

[RULE.ARCH.ALWAYS_WINS_CONSUMERS_MUST_NOT_AWARE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L430
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Resolved LoggingPolicy
  statement:
    ALWAYS wins. - Consumers MUST NOT be aware of aliases.

[RULE.ARCH.ANONYMOUS_HIDDEN_COMMAND_REGISTRATION_FORBIDDEN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L719
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.8 User Visibility
  statement:
    -   Anonymous or hidden command registration is forbidden.

[RULE.ARCH.ANY_ATTEMPT_INTERACT_USER_DURING_PHASE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L168
  heading_path: AudioMason2 - Project Specification (Authoritative) > 4. Execution Model (Strict Contract) > 4.2 Phase Enforcement
  statement:
    -   Any attempt to interact with the user during PHASE 2 must:

[RULE.ARCH.ANY_COLLISION_MUST_RESULT_DETERMINISTIC_ERROR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L693
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.5 Command Name Rules
  statement:
    -   Any collision MUST result in a deterministic error.

[RULE.ARCH.ANY_IMPLEMENTATION_PATCH_REFACTOR_FEATURE_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L36
  heading_path: AudioMason2 - Project Specification (Authoritative) > 1. Purpose of This Document
  statement:
    Any implementation, patch, refactor, or feature **must comply with this

[RULE.ARCH.ARCHIVE_CAPABILITY_MUST_ABLE_EMIT_STRUCTURED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L626
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   The archive capability must be able to emit a structured operation

[RULE.ARCH.AT_CLI_STARTUP_ONLY_STUBS_MAY]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L803
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    At CLI startup: - only stubs MAY be registered, - plugin code MUST NOT

[RULE.ARCH.AT_CLI_STARTUP_SYSTEM_MUST_PERFORM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L682
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.4 Loading Model
  statement:
    -   At CLI startup, the system MUST perform lightweight discovery

[RULE.ARCH.AT_SESSION_CREATION_ENGINE_MUST_GENERATE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L915
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.1 Workflow Authority (WizardDefinition)
  statement:
    At session creation, the engine MUST generate and persist the effective

[RULE.ARCH.AUDIOMASON_IMPORT_EDITOR_MUST_REMAIN_SUPPORTED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L989
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    and `audiomason import editor ...` MUST remain supported and unchanged.

[RULE.ARCH.AUDIOMASON_IMPORT_MUST_NOT_EMIT_HIGH]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L315
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.A Console Output Policy (Normative)
  statement:
    (`audiomason import`) MUST NOT emit high-volume internal INFO logs from

[RULE.ARCH.BEHAVIOR_MUST_NOT_START_RENDERER]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L997
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    behavior) and MUST NOT start the renderer. -

[RULE.ARCH.BEHAVIOR_SETTINGS_MUST_STORED_FLOWCONFIG_SEE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1260
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    behavior settings MUST be stored in FlowConfig (see 30_implementation_bindings.md):

[RULE.ARCH.BOOL_MUST_BOOL_NO_IMPLICIT_STRING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L298
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.6 Minimal Type Validation (Known Keys)
  statement:
    -   bool: must be bool (no implicit string coercion unless schema

[RULE.ARCH.BUILT_PLUGIN_LOADING_MUST_WORK_WITHOUT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L502
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.3.1 Built-in Plugin Import Path Rules (Loader Responsibility)
  statement:
    -   Built-in plugin loading MUST work without requiring the user to set

[RULE.ARCH.CACHES_IF_IMPLEMENTED_CACHE_KEYS_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1079
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    Caches (if implemented): - cache keys MUST be deterministic - cache miss

[RULE.ARCH.CALLABLES_MUST_ACCEPT_SINGLE_ARGUMENT_ARGV]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L747
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   Callables MUST accept a single argument: `argv: list[str]`.

[RULE.ARCH.CAPABILITY_MUST_PROVIDE_AT_MINIMUM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L534
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.2 Operations
  statement:
    The capability must provide, at minimum:

[RULE.ARCH.CHECKSUM_MUST_DETERMINISTIC_SHA256_OVER_FILE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L569
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.3 Determinism Rules
  statement:
    -   checksum must be deterministic (sha256 over file bytes).

[RULE.ARCH.CLI_COMMAND_REGISTRATION_MUST_RESPECT_PLUGINREGISTRY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L707
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.7 Enable / Disable Semantics
  statement:
    CLI command registration MUST respect PluginRegistry.

[RULE.ARCH.CLI_HELP_OUTPUT_MUST_DISTINGUISH_CORE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L827
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.14 Help and User-Facing Output Format
  statement:
    The CLI help output MUST distinguish core commands from plugin commands.

[RULE.ARCH.CLI_HOST_MUST_NOT_CALL_GET]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L751
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   The CLI host MUST NOT call `get_cli_commands()` until the command is

[RULE.ARCH.CLI_HOST_MUST_PRESERVE_THIS_SOURCE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L766
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    The CLI host MUST preserve this source ordering when iterating

[RULE.ARCH.CLI_HOST_MUST_TRANSPARENTLY_SUPPORT_BOTH]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L749
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   The CLI host MUST transparently support both sync and async

[RULE.ARCH.CLI_INPUT_RULES_CLI_MUST_SET]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L432
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Resolved LoggingPolicy
  statement:
    CLI input rules: - CLI MUST set the canonical key logging.level (nested:

[RULE.ARCH.CLI_MUST_ABORT_STARTUP_CLEAR_ERROR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L784
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    -   the CLI MUST abort startup with a clear error message.

[RULE.ARCH.CLI_PLUGIN_MUST_MAINTAIN_INTERNAL_REGISTRY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L788
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    The CLI plugin MUST maintain an internal registry of CLI command stubs.

[RULE.ARCH.COMMAND_INVOCATION_CLI_MUST_LOAD_EXACTLY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L806
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    On command invocation: - the CLI MUST load exactly one plugin, - call

[RULE.ARCH.COMMAND_NAMES_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L742
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   Command names MUST:

[RULE.ARCH.COMMAND_NAME_COLLISIONS_FORBIDDEN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L692
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.5 Command Name Rules
  statement:
    -   Command name collisions are FORBIDDEN.

[RULE.ARCH.COMMAND_STUB_MUST_CONTAIN_COMMAND_NAME]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L790
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    A command stub MUST contain: - command name, - providing plugin

[RULE.ARCH.CONDITIONAL_BRANCHING_MUST_LIVE_WIZARD_ENGINE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L979
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.5 Renderer Neutrality (Hard Rule)
  statement:
    conditional branching) MUST live in the wizard engine.

[RULE.ARCH.CONFIGRESOLVER_MUST_EXPOSE_AUTHORITATIVE_SCHEMA_REGISTRY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L257
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.3 Configuration Schema Registry
  statement:
    -   ConfigResolver must expose an authoritative schema registry for

[RULE.ARCH.CONFIGSERVICE_MUST_RE_INITIALIZE_RESOLVER_AFTER]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L387
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.8 ConfigService Mutation Primitives
  statement:
    ConfigService must re-initialize the resolver after each mutation so

[RULE.ARCH.CONFIGSOURCE_MUST_INCLUDE_SOURCE_LEVEL_NAME]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L412
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Resolved LoggingPolicy
  statement:
    ConfigSource\] - Must include the source for level_name under the key

[RULE.ARCH.CONFIGURATION_IT_MUST_USE_CONFIGSERVICE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L372
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.8 ConfigService Mutation Primitives
  statement:
    configuration, it must use ConfigService.

[RULE.ARCH.CONFIGURATION_RESOLVER_MUST_PROVIDE_CANONICAL_STRUCTURED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L403
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Resolved LoggingPolicy
  statement:
    The configuration resolver MUST provide a canonical, structured logging

[RULE.ARCH.CORE_MUST_NEVER_IMPLEMENT_AUDIO_PROCESSING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L105
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.1 Ultra?Minimal Core
  statement:
    Core must **never**: - implement audio processing - implement metadata

[RULE.ARCH.CORE_MUST_NOT_IMPLEMENT_STORAGE_BACKEND]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L518
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned)
  statement:
    -   Core must not implement a storage backend; file I/O remains

[RULE.ARCH.CORE_MUST_NOT_PROVIDE_GLOBAL_CLI]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L652
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.1 Ownership and Responsibility
  statement:
    -   Core MUST NOT provide a global CLI hook bus.

[RULE.ARCH.CORE_MUST_REMAIN_SMALL_READABLE_STABLE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L97
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.1 Ultra?Minimal Core
  statement:
    -   Core must remain small, readable, and stable.

[RULE.ARCH.DEFAULTS_DISABLED_AUDIOMASON_IMPORT_MUST_PRINT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L996
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    defaults. - `disabled`: `audiomason import` MUST print usage (legacy

[RULE.ARCH.DEFAULT_WORKFLOW_PROVIDED_SYSTEM_SHALL_REMAIN]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L944
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.3 Default Workflow
  statement:
    The default workflow provided by the system SHALL remain strictly linear

[RULE.ARCH.DEFINE_WHAT_AUDIOMASON2_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L30
  heading_path: AudioMason2 - Project Specification (Authoritative) > 1. Purpose of This Document
  statement:
    -   define **what AudioMason2 is and must be**,

[RULE.ARCH.DEPRECATED_PLUGINS_SYSLOG_FILENAME_MUST_NOT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L367
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.2 Syslog plugin
  statement:
    Deprecated: - plugins.syslog.filename (must not be used anymore)

[RULE.ARCH.DIAGNOSTICS_MUST_INCLUDE_AT_MINIMUM_SESSION]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L1097
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.14 Diagnostics (Mandatory)
  statement:
    Diagnostics MUST include at minimum: - session_id - model_fingerprint -

[RULE.ARCH.DICTIONARY_KEYS_MUST_CLI_COMMAND_NAMES]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L741
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   Dictionary keys MUST be CLI command names.

[RULE.ARCH.DICTIONARY_VALUES_MUST_CALLABLES]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L746
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    -   Dictionary values MUST be callables.

[RULE.ARCH.DISABLED_PLUGINS_MUST_NOT_REGISTER_CLI]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L709
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.7 Enable / Disable Semantics
  statement:
    -   Disabled plugins MUST NOT register CLI commands.

[RULE.ARCH.DISCOVERED_PLUGIN_DIRECTORIES_CLI_HOST_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L767
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    discovered plugin directories. The CLI host MUST NOT globally

[RULE.ARCH.DISCOVERY_MUST_DETERMINISTIC_INDEPENDENT_FILESYSTEM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L675
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.3 Discovery Sources
  statement:
    Discovery MUST be deterministic and independent of filesystem

[RULE.ARCH.EACH_CHANGE_ENTRY_DOCS_CHANGES_MD]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L56
  heading_path: AudioMason2 - Project Specification (Authoritative) > Specification Versioning and Change Log
  statement:
    -   Each change entry in docs/changes.md MUST start with an ISO 8601

[RULE.ARCH.EACH_SCHEMA_ENTRY_MUST_DEFINE_AT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L259
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.3 Configuration Schema Registry
  statement:
    -   Each schema entry must define at minimum:

[RULE.ARCH.EDGE_MAY_CONTAIN_CONDITION_OBJECT_IF]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1223
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    An edge MAY contain a condition object. If condition is missing, it MUST

[RULE.ARCH.ENGINE_MUST_ENFORCE_MANDATORY_CONSTRAINTS_PRESERVE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L925
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.2 Mandatory Steps and Ordering Constraints
  statement:
    engine MUST enforce mandatory constraints to preserve phases and safety

[RULE.ARCH.ENGINE_MUST_NORMALIZE_V1_V2_AS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1215
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - The engine MUST normalize v1 to v2 as a linear graph during validation.

[RULE.ARCH.ENGINE_MUST_PERFORM_CONFLICT_SCAN]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L968
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.4 Conditional Conflict Path
  statement:
    -   Engine MUST perform a conflict scan.

[RULE.ARCH.ENGINE_MUST_TRANSITION_BACK_SELECT_BOOKS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L956
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.3 Default Workflow
  statement:
    the engine MUST transition back to select_books.

[RULE.ARCH.ENGINE_MUST_TREAT_STRUCTURE_WIZARDDEFINITION_BEHAVIOR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1263
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - The engine MUST treat structure (WizardDefinition) and behavior

[RULE.ARCH.ENGINE_SIDE_INVARIANTS_MUST_ENFORCED_DURING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1211
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - Engine-side invariants MUST be enforced during validate/save.

[RULE.ARCH.ENUM_MUST_ONE_ALLOWED_ENUM_VALUES]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L300
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.6 Minimal Type Validation (Known Keys)
  statement:
    -   enum: must be one of the allowed enum values

[RULE.ARCH.ERR_PAYLOAD_STATE_MUST_NOT_ADVANCE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L949
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.3 Default Workflow
  statement:
    -   ERR(payload): state MUST NOT advance; the current_step_id remains

[RULE.ARCH.EVALUATION_MUST_TOTAL_DETERMINISTIC_NO_EXCEPTIONS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1245
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - Evaluation MUST be total and deterministic (no exceptions).

[RULE.ARCH.EVENTS_MUST_INCLUDE_AT_MINIMUM_JOB]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L222
  heading_path: AudioMason2 - Project Specification (Authoritative) > 5. Job Model (Mandatory) > 5.4 Job Observability (Mandatory)
  statement:
    Events MUST include at minimum: - job_id - job_type - state - progress

[RULE.ARCH.EVERY_CHANGE_DELIVERED_PATCH_MUST_RECORDED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L54
  heading_path: AudioMason2 - Project Specification (Authoritative) > Specification Versioning and Change Log
  statement:
    -   Every change delivered by a patch MUST be recorded in

[RULE.ARCH.EVERY_CHANGE_THIS_SPECIFICATION_MUST_INCREMENT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L52
  heading_path: AudioMason2 - Project Specification (Authoritative) > Specification Versioning and Change Log
  statement:
    -   Every change to this specification MUST increment the patch number

[RULE.ARCH.EVERY_EDGE_FROM_EDGE_MUST_REFERENCE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1206
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - Every edge.from and edge.to MUST reference an existing nodes.step_id.

[RULE.ARCH.FAILURE_CLI_COMMAND_PLUGIN_MUST_NOT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L698
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.6 Failure Isolation
  statement:
    Failure of a CLI command plugin MUST NOT crash the CLI.

[RULE.ARCH.FAILURE_MUST_NOT_CRASH_CLI_PROCESS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L817
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.13 Failure Semantics for Command Invocation
  statement:
    -   the failure MUST NOT crash the CLI process,

[RULE.ARCH.FAILURE_ONE_PLUGIN_MUST_NOT_AFFECT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L822
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.13 Failure Semantics for Command Invocation
  statement:
    Failure of one plugin MUST NOT affect discovery or execution of other

[RULE.ARCH.FILESYSTEM_ITERATION_ORDER_MUST_NOT_INFLUENCE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L779
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    -   filesystem iteration order MUST NOT influence behavior.

[RULE.ARCH.FILESYSTEM_LISTING_PERFORMED_RENDERER_MUST_DONE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1022
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    filesystem listing performed by the renderer MUST be done via

[RULE.ARCH.FILESYSTEM_ORDER_MUST_NOT_AFFECT_BEHAVIOR]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L726
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.9 Determinism Requirements
  statement:
    -   Filesystem order MUST NOT affect behavior.

[RULE.ARCH.FILE_I_O_CAPABILITY_ITS_ROOTS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L338
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.1 Human-Readable System Log File
  statement:
    the File I/O capability (and its roots). UI layers must not write to

[RULE.ARCH.FILE_I_O_CAPABILITY_MUST_NOT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L555
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.2 Operations
  statement:
    -   The file I/O capability must not implement rotation, size limits, or

[RULE.ARCH.FILE_I_O_CAPABILITY_MUST_NOT_2]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L562
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.2 Operations
  statement:
    -   The file I/O capability must not implement follow, rotation,

[RULE.ARCH.FILE_I_O_CAPABILITY_MUST_SUPPORT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L523
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.1 Roots
  statement:
    The file I/O capability must support named roots:

[RULE.ARCH.FILE_I_O_PLUGIN_MUST_PROVIDE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L582
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    The file I/O plugin must provide an archive capability built on top of

[RULE.ARCH.FILE_SERVICE_ITSELF_MUST_NOT_GENERATE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L570
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.3 Determinism Rules
  statement:
    -   The file service itself must not generate random or time-based

[RULE.ARCH.FLOWCONFIG_MUST_NOT_MODIFY_STRUCTURE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1142
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.16 Structural Workflow Authority (WizardDefinition Model)
  statement:
    -   FlowConfig MUST NOT modify structure.

[RULE.ARCH.FLOWMODEL_BUILD_MUST_LIGHTWEIGHT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1070
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    -   FlowModel build must be lightweight.

[RULE.ARCH.FULL_PLUGIN_IMPORT_INITIALIZATION_MUST_NOT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L684
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.4 Loading Model
  statement:
    -   Full plugin import and initialization MUST NOT occur until the

[RULE.ARCH.HELP_OUTPUT_MUST_INDICATE_PROVIDING_PLUGIN]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L717
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.8 User Visibility
  statement:
    -   Help output MUST indicate the providing plugin for each

[RULE.ARCH.HIGHER_LAYER_MUST_ABLE_CHOOSE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L601
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   The higher layer must be able to choose:

[RULE.ARCH.HIGHER_LAYER_MUST_ABLE_SPECIFY_DESTINATION]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L599
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   The higher layer must be able to specify the destination directory

[RULE.ARCH.HIGHER_LAYER_MUST_ABLE_SPECIFY_EXACT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L597
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   The higher layer must be able to specify the exact archive filename

[RULE.ARCH.HYBRID_LOADING_MODEL_REQUIRED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L680
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.4 Loading Model
  statement:
    A hybrid loading model is required:

[RULE.ARCH.IF_ICLICOMMANDS_NOT_DECLARED_CLI_COMMANDS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L801
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    -   If `ICLICommands` is not declared, `cli_commands` MUST be ignored.

[RULE.ARCH.IF_IMPLEMENTED_MUST_NOT_UPDATE_IMPLICITLY]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1061
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    (if implemented) MUST NOT update implicitly. - Only explicit user action

[RULE.ARCH.IMPLICIT_HEURISTIC_DETECTION_FORBIDDEN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L664
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.2 Declaration Contract
  statement:
    -   Implicit or heuristic detection is forbidden.

[RULE.ARCH.IMPORT_ENGINE_MUST_EMIT_VIA_CORE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L1085
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.14 Diagnostics (Mandatory)
  statement:
    The import engine MUST emit via the Core authoritative diagnostic entry

[RULE.ARCH.IMPORT_WIZARD_FLOW_MUST_REPRESENTABLE_AS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1175
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    The Import wizard flow MUST be representable as a directed graph. The

[RULE.ARCH.IMPORT_WIZARD_MUST_USE_SINGLE_INTERPRETER]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1153
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine)
  statement:
    The Import Wizard MUST use a single interpreter responsible for:

[RULE.ARCH.IMPORT_WIZARD_STRUCTURAL_WORKFLOW_MUST_DEFINED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1128
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.16 Structural Workflow Authority (WizardDefinition Model)
  statement:
    The Import Wizard structural workflow MUST be defined by a single

[RULE.ARCH.INSTALLATION_MECHANISM_MUST_ABSTRACTED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L498
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.3 Plugin Installation Rules
  statement:
    -   Installation mechanism must be abstracted.

[RULE.ARCH.INT_MUST_INT_NO_IMPLICIT_STRING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L296
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.6 Minimal Type Validation (Known Keys)
  statement:
    -   int: must be int (no implicit string coercion unless schema

[RULE.ARCH.IT_MUST_CONTAIN_ONLY_ISO_8601]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L63
  heading_path: AudioMason2 - Project Specification (Authoritative) > Specification Versioning and Change Log > docs/changes.md - Canonical Change Log Format (MANDATORY)
  statement:
    It MUST contain: - only ISO 8601 timestamps and human-readable

[RULE.ARCH.IT_MUST_NOT_CONTAIN_ISSUE_NUMBERS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L68
  heading_path: AudioMason2 - Project Specification (Authoritative) > Specification Versioning and Change Log > docs/changes.md - Canonical Change Log Format (MANDATORY)
  statement:
    It MUST NOT contain: - issue numbers or issue references, - issue

[RULE.ARCH.JOBS_MUST_EMIT_LIFECYCLE_DIAGNOSTICS_THROUGH]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L210
  heading_path: AudioMason2 - Project Specification (Authoritative) > 5. Job Model (Mandatory) > 5.4 Job Observability (Mandatory)
  statement:
    Jobs MUST emit lifecycle diagnostics through the authoritative EventBus.

[RULE.ARCH.JOBS_MUST_PERSISTED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L204
  heading_path: AudioMason2 - Project Specification (Authoritative) > 5. Job Model (Mandatory) > 5.3 Job Persistence
  statement:
    -   Jobs must be persisted.

[RULE.ARCH.JOBS_MUST_PERSISTED_2]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L225
  heading_path: AudioMason2 - Project Specification (Authoritative) > 5. Job Model (Mandatory) > 5.4 Job Observability (Mandatory)
  statement:
    -   Jobs must be persisted.

[RULE.ARCH.KNOWN_KEYS_PRESENT_SCHEMA_MUST_SUPPORT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L295
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.6 Minimal Type Validation (Known Keys)
  statement:
    -   Known keys (present in schema) must support minimal type validation:

[RULE.ARCH.LIST_DIR_ORDERING_MUST_STABLE_DETERMINISTIC]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L567
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.3 Determinism Rules
  statement:
    -   list_dir ordering must be stable and deterministic (lexicographic by

[RULE.ARCH.LOGGING_LEVEL_CLI_MUST_NOT_SET]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L433
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Resolved LoggingPolicy
  statement:
    logging.level). - CLI MUST NOT set the resolver-only alias verbosity.

[RULE.ARCH.LONG_RUNNING_OPERATIONS_MUST_ASYNCHRONOUS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L135
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.4 Asynchronous by Design
  statement:
    -   Long?running operations must be asynchronous.

[RULE.ARCH.MANDATORY_ORDERING_CONSTRAINTS_MUST_HOLD_SELECT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L933
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.2 Mandatory Steps and Ordering Constraints
  statement:
    Mandatory ordering constraints (MUST hold): - select_authors precedes

[RULE.ARCH.MANDATORY_STEP_IDS_MUST_EXIST_EVERY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L928
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.2 Mandatory Steps and Ordering Constraints
  statement:
    Mandatory step_ids (MUST exist in every effective workflow): S0

[RULE.ARCH.MANIFEST_ONLY_DISCOVERY_MUST_NOT_EXECUTE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L686
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.4 Loading Model
  statement:
    -   Manifest-only discovery MUST NOT execute plugin code.

[RULE.ARCH.MAY_UPDATE_DEFAULTS_ANY_AUTO_SAVE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1062
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    may update defaults. - Any auto-save (if implemented) MUST be visible

[RULE.ARCH.MULTIPLE_PARALLEL_PLUGIN_STATE_MECHANISMS_FORBIDDEN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L452
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.1 Plugin Registry
  statement:
    Multiple parallel plugin state mechanisms are forbidden.

[RULE.ARCH.MUST_EVALUATED_ENGINE_RENDERER_NEUTRAL]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1177
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    MUST be evaluated by the engine (renderer-neutral).

[RULE.ARCH.MUST_NOT_MODIFY_OVERRIDE_INTERCEPT_DISABLE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L643
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension
  statement:
    MUST NOT modify, override, intercept, or disable existing CLI commands.

[RULE.ARCH.MUST_RECOMPUTE_NEVER_SILENTLY_FAIL_ENABLING]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L1080
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    MUST recompute (never silently fail) - enabling/disabling cache MUST NOT

[RULE.ARCH.MUST_STILL_APPEAR_HELP_OUTPUT_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L837
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.14 Help and User-Facing Output Format
  statement:
    MUST still appear in help output, - and MUST be annotated as

[RULE.ARCH.NODES_STEP_ID_MUST_UNIQUE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1205
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - nodes.step_id MUST be unique.

[RULE.ARCH.NORMALIZATION_RULES_VALUE_MUST_STRING_TRIM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L390
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.8 ConfigService Mutation Primitives
  statement:
    Normalization rules: - value must be a string - trim whitespace -

[RULE.ARCH.OBSOLETE_UNSUPPORTED_MUST_NOT_USED_PLUGIN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L481
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.1 Plugin Registry > 7.1.3 Obsolete plugins.yaml
  statement:
    obsolete/unsupported and MUST NOT be used for plugin state or plugin

[RULE.ARCH.OK_PAYLOAD_STATE_MUST_ADVANCE_DETERMINISTICALLY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L951
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.3 Default Workflow
  statement:
    -   OK(payload): state MUST advance deterministically to the next step

[RULE.ARCH.ORDERING_HELP_OUTPUT_MUST_DETERMINISTIC_STABLE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L833
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.14 Help and User-Facing Output Format
  statement:
    The ordering of help output MUST be deterministic and stable across

[RULE.ARCH.ORIGIN_EVERY_PLUGIN_PROVIDED_CLI_COMMAND]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L714
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.8 User Visibility
  statement:
    The origin of every plugin-provided CLI command MUST be visible to the

[RULE.ARCH.OTHER_CLI_COMMANDS_MUST_REMAIN_AVAILABLE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L702
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.6 Failure Isolation
  statement:
    -   Other CLI commands MUST remain available.

[RULE.ARCH.OTHER_CLI_COMMANDS_MUST_REMAIN_FUNCTIONAL]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L820
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.13 Failure Semantics for Command Invocation
  statement:
    -   other CLI commands MUST remain functional.

[RULE.ARCH.PACKAGE_CORE_PLUGIN_LOADER_MUST_ENSURE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L505
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.3.1 Built-in Plugin Import Path Rules (Loader Responsibility)
  statement:
    package, the core plugin loader MUST ensure the repository root is

[RULE.ARCH.PARALLELISM_MUST_BOUNDED_HARD_MAX_PARALLELISM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1076
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    -   parallelism MUST be bounded by a hard MAX_PARALLELISM limit in the

[RULE.ARCH.PATH_MUST_START_INPUTS_STATE_READ]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1237
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - path MUST start with $.inputs. or $.state. (read-only view).

[RULE.ARCH.PERFORMANCE_RULES_NON_FUNCTIONAL_CONSTRAINTS_BUT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L1067
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    Performance rules are non-functional constraints but MUST NOT alter

[RULE.ARCH.PERSISTED_ARTIFACTS_MUST_NOT_INCLUDE_TIMESTAMPS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1053
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    Persisted artifacts MUST NOT include timestamps or editor metadata

[RULE.ARCH.PERSISTED_WIZARDDEFINITION_MUST_NOT_INCLUDE_TIMESTAMPS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1210
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - Persisted WizardDefinition MUST NOT include timestamps or editor metadata.

[RULE.ARCH.PHASE_3_IMPLEMENTATION_REQUIREMENT_BUILT_CLI]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L810
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    Phase 3 implementation requirement: - The built-in CLI plugin MUST

[RULE.ARCH.PLAN_PREVIEW_BATCH_OUTPUT_MUST_BOUNDED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1072
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    -   plan_preview_batch output MUST be bounded by preview_limit and MUST

[RULE.ARCH.PLUGINS_IMPORT_CLI_NONINTERACTIVE_IF_TRUE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1001
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    `plugins.import.cli.noninteractive`: if true, the renderer MUST NOT

[RULE.ARCH.PLUGINS_MUST_ISOLATABLE_REMOVABLE_WITHOUT_BREAKING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L117
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.2 Plugin?First Architecture
  statement:
    -   Plugins must be isolatable and removable without breaking the

[RULE.ARCH.PLUGINS_MUST_NOT_ASSUME_FILESYSTEM_LAYOUT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L490
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.2 Plugin Isolation
  statement:
    -   Plugins must not assume filesystem layout or config storage.

[RULE.ARCH.PLUGINS_MUST_NOT_OVERRIDE_SHADOW_CORE]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L846
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.15 Relationship to Core CLI Dispatch
  statement:
    Plugins MUST NOT override or shadow core commands.

[RULE.ARCH.PLUGINS_MUST_ORDERED_MANIFEST_NAME_LEXICOGRAPHICALLY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L778
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    -   plugins MUST be ordered by `manifest.name` (lexicographically).

[RULE.ARCH.PLUGINS_THAT_DECLARE_ICLICOMMANDS_INTERFACE_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L732
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    Plugins that declare the `ICLICommands` interface MUST implement the

[RULE.ARCH.PLUGINS_THAT_DO_NOT_DECLARE_THIS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L662
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.2 Declaration Contract
  statement:
    -   Plugins that do not declare this interface MUST be ignored for CLI

[RULE.ARCH.PLUGINS_THAT_PROVIDE_CLI_COMMANDS_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L658
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.2 Declaration Contract
  statement:
    Plugins that provide CLI commands MUST explicitly declare this

[RULE.ARCH.PLUGIN_CLASS_MUST_EXPOSE_METHOD]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/10_architecture.md#L735
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.10 ICLICommands Interface Contract
  statement:
    The plugin class MUST expose a method:

[RULE.ARCH.PLUGIN_CONFIGURATION_HOST_CONFIGURATION_MUST_STORED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L456
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.1 Plugin Registry > 7.1.1 Canonical plugin configuration key-space (host config)
  statement:
    Plugin configuration is host configuration and MUST be stored under:

[RULE.ARCH.PLUGIN_DIRECTORIES_MUST_SORTED_LEXICOGRAPHICALLY_DIRECTORY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L772
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    -   plugin directories MUST be sorted lexicographically by directory

[RULE.ARCH.PLUGIN_FAILURE_MUST_NOT_CRASH_SYSTEM]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L488
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.2 Plugin Isolation
  statement:
    -   Plugin failure must not crash the system.

[RULE.ARCH.PLUGIN_MUST_MARKED_AS_FAILED_CURRENT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L818
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.13 Failure Semantics for Command Invocation
  statement:
    -   the plugin MUST be marked as failed for the current session,

[RULE.ARCH.PLUGIN_MUST_VALIDATE_THAT_LOGGING_SYSTEM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L364
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.2 Syslog plugin
  statement:
    The plugin MUST validate that logging.system_log_path resolves under the

[RULE.ARCH.PLUGIN_PROCESSING_ORDER_MUST_EXPLICITLY_DEFINED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L725
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.9 Determinism Requirements
  statement:
    -   Plugin processing order MUST be explicitly defined and stable.

[RULE.ARCH.PLUGIN_PROVIDED_CLI_COMMAND_NAMES_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L690
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.5 Command Name Rules
  statement:
    Plugin-provided CLI command names MUST be globally unique.

[RULE.ARCH.PLUGIN_THAT_FAILS_DURING_LOADING_EXECUTION]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L700
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5 CLI Plugin Command Extension > 7.5.6 Failure Isolation
  statement:
    -   A plugin that fails during loading or execution MUST be disabled for

[RULE.ARCH.PLUGIN_YAML_MUST_DECLARE_COMMAND_NAMES]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L797
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    (`plugin.yaml`) MUST declare the command names under

[RULE.ARCH.PROCESSING_MUST_REMAIN_TERMINAL_STEP]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1219
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - processing MUST remain the terminal step.

[RULE.ARCH.PRODUCES_PHASE_2_JOB_REQUESTS_ONLY]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L874
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.1 Identity and Scope
  statement:
    -   Produces PHASE 2 job requests only; it MUST NOT execute PHASE 2

[RULE.ARCH.PROGRESS_LOGS_MUST_OBSERVABLE_WHILE_WORK]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L137
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.4 Asynchronous by Design
  statement:
    -   Progress and logs must be observable while work is running.

[RULE.ARCH.PROMPT_MUST_FAIL_IF_REQUIRED_INPUTS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1002
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    prompt and MUST fail if required inputs are missing. -

[RULE.ARCH.RAR_UNPACK_REQUIRED_PACK_BEST_EFFORT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L614
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   RAR (unpack is required; pack is best-effort and may depend on

[RULE.ARCH.RENDERERS_CLI_WEB_MUST_NOT_CONTAIN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L977
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.5 Renderer Neutrality (Hard Rule)
  statement:
    Renderers (CLI/Web) MUST NOT contain import-specific branching logic

[RULE.ARCH.REPORTED_STDERR_SYSTEM_MUST_NOT_CRASH]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L334
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.1 Human-Readable System Log File
  statement:
    reported to stderr; the system must not crash.

[RULE.ARCH.REPOSITORY_MUST_INCLUDE_DETERMINISTIC_TESTS_THAT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1102
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.15 Testing and Enforcement (Hard Gates)
  statement:
    The repository MUST include deterministic tests that enforce:

[RULE.ARCH.REQUIRED_BEHAVIOR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L595
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    Required behavior:

[RULE.ARCH.RESERVED_CORE_COMMAND_NAMES_PLUGINS_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L848
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.15 Relationship to Core CLI Dispatch
  statement:
    Reserved core command names (plugins MUST NOT provide these names): -

[RULE.ARCH.RESOLVER_CONFIG_HARD_CODED_DEFAULTS_LAUNCHER]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1016
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    resolver config \> hard-coded defaults. - The launcher MUST provide a

[RULE.ARCH.RESOLVER_MUST_PROVIDE_DETERMINISTIC_LIST_KNOWN]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L274
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Deterministic Key Enumeration
  statement:
    -   Resolver must provide a deterministic list of known keys:

[RULE.ARCH.RESOLVER_VALUES_CURRENT_RUN_PRECEDENCE_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1015
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    resolver values for the current run. - Precedence MUST be: CLI flags \>

[RULE.ARCH.RESOLVE_ALL_MUST_ENUMERATE_AT_LEAST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L277
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.4 Deterministic Key Enumeration
  statement:
    -   `resolve_all()` must enumerate at least all known keys from the

[RULE.ARCH.ROOTS_PATH_MUST_RELATIVE_PATH_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1021
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    roots). - Path MUST be a relative path and MUST NOT contain `..`. - Any

[RULE.ARCH.ROOT_PATH_RULES_ROOT_MUST_VALIDATED]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1020
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    Root/path rules: - Root MUST be validated against RootName (file_io

[RULE.ARCH.RUNTIME_MUTATION_REPOSITORY_PLUGINS_FORBIDDEN]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: PLUGIN | CORE | UI
  source: docs/spec/10_architecture.md#L496
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.3 Plugin Installation Rules
  statement:
    -   Runtime mutation of repository plugins is forbidden.

[RULE.ARCH.SELECTION_PARSER_MUST_LINEAR_TIME]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1071
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.13 Performance and Scaling (Deterministic)
  statement:
    -   Selection parser must be linear time.

[RULE.ARCH.SELECT_AUTHORS_MUST_REMAIN_ENTRY_STEP]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1218
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - select_authors MUST remain the entry_step_id.

[RULE.ARCH.SORTING_KEY_MUST_DIRECTORY_NAME_PATH]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L774
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.11 Deterministic Discovery and Ordering Rules
  statement:
    -   Sorting key MUST be the directory name (Path.name).

[RULE.ARCH.SOURCE_EACH_RESOLVED_VALUE_MUST_TRACEABLE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L251
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.2 Priority Rules
  statement:
    The source of each resolved value must be traceable in debug mode.

[RULE.ARCH.SPECIAL_RULE_PLAN_PREVIEW_BATCH_ERROR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L954
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.3 Default Workflow
  statement:
    Special rule: plan_preview_batch error handling MUST be deterministic: -

[RULE.ARCH.STATE_MUST_REMAIN_FINAL_SUMMARY_CONFIRM]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L963
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.4 Conditional Conflict Path
  statement:
    -   The state MUST remain final_summary_confirm (user must set true

[RULE.ARCH.STEP_BEHAVIOR_SETTINGS_EDITS_MUST_ONLY]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1038
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    -   Step behavior/settings edits MUST only affect FlowConfig (for example: defaults[step_id]).

[RULE.ARCH.STRUCTURAL_EDITS_ADD_REMOVE_REORDER_STEPS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1037
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    -   Structural edits (add/remove/reorder steps) MUST only affect WizardDefinition.

[RULE.ARCH.SUBSCRIBERS_PLUGINS_THAT_SUBSCRIBE_LOGBUS_PERSISTENCE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L337
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.1 Human-Readable System Log File
  statement:
    subscribers (plugins) that subscribe to LogBus. - Persistence MUST use

[RULE.ARCH.SUBSEQUENT_INVOCATIONS_MUST_REJECTED_CLEAR_ERROR]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/10_architecture.md#L819
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.13 Failure Semantics for Command Invocation
  statement:
    -   subsequent invocations MUST be rejected with a clear error,

[RULE.ARCH.SUCH_LOGS_MUST_REMAIN_AVAILABLE_VIA]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L318
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.A Console Output Policy (Normative)
  statement:
    Such logs MUST remain available via LogBus.

[RULE.ARCH.SYNCHRONOUS_SHORTCUTS_FORBIDDEN_EXCEPT_TRIVIAL_OPERATIONS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L139
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.4 Asynchronous by Design
  statement:
    Synchronous shortcuts are forbidden except for trivial operations.

[RULE.ARCH.SYSTEM_MUST_REMAIN_FUNCTIONAL_IF_IMPORT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L881
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.1 Identity and Scope
  statement:
    -   The system MUST remain functional if the import plugin is disabled

[RULE.ARCH.TERMINAL_NODE_PROCESSING_PHASE_1_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1183
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - Terminal node: processing (Phase 1) (MUST remain terminal).

[RULE.ARCH.THERE_MUST_SINGLE_PLUGINREGISTRY_API]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L444
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.1 Plugin Registry
  statement:
    -   There must be a single **PluginRegistry API**.

[RULE.ARCH.THEY_MUST_NOT_IMPLEMENT_VALIDATION_TRANSITIONS]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARCH
  source: docs/spec/10_architecture.md#L878
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.1 Identity and Scope
  statement:
    -   They MUST NOT implement validation, transitions, conflict

[RULE.ARCH.THIS_CAPABILITY_MUST_SUPPORT]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L585
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    This capability must support:

[RULE.ARCH.THIS_LIST_MUST_USED_BUILD_STUB]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/10_architecture.md#L799
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.5.12 CLI Command Stub Registry
  statement:
    -   This list MUST be used to build the stub registry and help output

[RULE.ARCH.UI_LAYERS_CLI_WEB_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L1161
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine)
  statement:
    UI layers (CLI/Web) MUST:

[RULE.ARCH.UI_LAYERS_CLI_WEB_TUI_MUST]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L516
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned)
  statement:
    -   UI layers (CLI, Web, TUI) must not implement filesystem logic; they

[RULE.ARCH.UI_LAYERS_MUST_NOT_EDIT_RAW]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L371
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.8 ConfigService Mutation Primitives
  statement:
    UI layers must not edit raw YAML directly. If a UI needs to mutate user

[RULE.ARCH.UI_MUST_MAINTAIN_TWO_INDEPENDENT_DRAFTS]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1036
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    -   The UI MUST maintain two independent drafts and persist them separately.

[RULE.ARCH.UI_MUST_NEVER_BLOCK_PROCESSING]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARCH
  source: docs/spec/10_architecture.md#L136
  heading_path: AudioMason2 - Project Specification (Authoritative) > 3. Fundamental Principles (Non?Negotiable) > 3.4 Asynchronous by Design
  statement:
    -   UI must never block on processing.

[RULE.ARCH.UI_ONLY_IT_MUST_DELEGATE_VALIDATION]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L984
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.3 Authoritative State Machine (Steps and Transitions) > 10.3.6 CLI Launcher and Renderer Configuration (Normative)
  statement:
    is UI-only. It MUST delegate validation, transitions, conflict logic,

[RULE.ARCH.UNKNOWN_KEYS_MUST_NOT_MAKE_RESOLVER]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L289
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.5 Unknown Keys Policy (Compatibility)
  statement:
    -   Unknown keys must not make the resolver non-deterministic.

[RULE.ARCH.USER_SYSTEM_PLUGIN_DIRECTORIES_MUST_NOT]
  rule_layer: ARCH_RULE
  normativity: MUST_NOT
  scope: ENDPOINT
  source: docs/spec/10_architecture.md#L509
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.3.1 Built-in Plugin Import Path Rules (Loader Responsibility)
  statement:
    user/system plugin directories MUST NOT implicitly grant

[RULE.ARCH.VALIDATE_ALL_UI_MUST_VALIDATE_BOTH]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1039
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.12 Config Governance (Editors and Storage)
  statement:
    -   Validate All: the UI MUST validate both artifacts before allowing Save All.

[RULE.ARCH.VALUE_LIST_VALUES_MUST_JSON_PRIMITIVES]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1238
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.17 Interpreter Authority (Single Execution Engine) > FlowGraph Branching Model (WizardDefinition v2) (Normative)
  statement:
    - value and list values MUST be JSON primitives (string, integer, boolean).

[RULE.ARCH.WARNINGS_ERRORS_MUST_REMAIN_VISIBLE_NORMAL]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L322
  heading_path: AudioMason2 - Project Specification (Authoritative) > 6. Configuration System > 6.7.A Console Output Policy (Normative)
  statement:
    Warnings and errors MUST remain visible in `normal`.

[RULE.ARCH.WHEN_FLATTENING_HIGHER_LAYER_MUST_ABLE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/10_architecture.md#L604
  heading_path: AudioMason2 - Project Specification (Authoritative) > 7. Plugin System Specification > 7.4 File I/O Capability (Plugin-Owned) > 7.4.5 Archive Capability (Plugin-Owned)
  statement:
    -   When flattening, the higher layer must be able to select a collision

[RULE.ARCH.WIZARDDEFINITION_MUST_VERSIONED_VALIDATED_LOAD]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L1144
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.16 Structural Workflow Authority (WizardDefinition Model)
  statement:
    -   WizardDefinition MUST be versioned and validated on load.

[RULE.ARCH.WIZARD_MUST_FOLLOW_GLOBAL_THREE_PHASE]
  rule_layer: ARCH_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/10_architecture.md#L886
  heading_path: AudioMason2 - Project Specification (Authoritative) > 10.2 Three-Phase Enforcement (Import Wizard)
  statement:
    The wizard MUST follow the global three-phase model:

[RULE.LAYOUT.10_11_JOB_REQUESTS_JSON_MUST]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L70
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    (10.11). - job_requests.json MUST be created only when start_processing

[RULE.LAYOUT.10_9_ALL_WRITES_MUST_ATOMIC]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L79
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    (10.9). All writes MUST be atomic (write temp, then rename).

[RULE.LAYOUT.ACTIONS_CONTRACT_NORMATIVE_ACTIONS_MUST_DERIVED]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L192
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    actions\[\] contract (normative): - actions\[\] MUST be derived from

[RULE.LAYOUT.ACTION_JOBS_JSON_MUST_NOT_USED]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L43
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    -   action_jobs.json MUST NOT be used for PHASE 2 processing jobs

[RULE.LAYOUT.ACTION_STEPS_EXECUTION_JOB_INTERPRETER_MUST]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L158
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    For action steps with execution == "job", the interpreter MUST create

[RULE.LAYOUT.ACTIVE_SESSIONS_MUST_USE_THEIR_SNAPSHOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L136
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.9 Session Snapshot Isolation and Persistence Model
  statement:
    Active sessions MUST use their snapshot for their entire lifetime.

[RULE.LAYOUT.ALL_FILE_REFERENCES_MUST_USE_ROOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L189
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    All file references MUST use (root, relative_path). Absolute paths are

[RULE.LAYOUT.ALL_IMPORT_WIZARD_DATA_MUST_STORED]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L12
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    All Import Wizard data MUST be stored under the file_io root:

[RULE.LAYOUT.ALL_JOB_REQUEST_SERIALIZATION_MUST_CANONICAL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L204
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.1 Canonical Serialization (Mandatory)
  statement:
    All job request serialization MUST be canonical: - JSON keys sorted -

[RULE.LAYOUT.ANY_DEVIATION_FROM_CANONICAL_JOB_REQUEST]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L178
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    Any deviation from the canonical Job Request schema SHALL be treated as

[RULE.LAYOUT.AT_SESSION_START_ENGINE_MUST_CREATE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L132
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.9 Session Snapshot Isolation and Persistence Model
  statement:
    At session start, the engine MUST create an immutable snapshot: -

[RULE.LAYOUT.BOOTSTRAP_RULES_CREATION_MUST_ATOMIC_WRITE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L87
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.1 Model Bootstrap When Missing
  statement:
    Bootstrap rules: - Creation MUST be atomic (write temp, then rename). -

[RULE.LAYOUT.BUNDLE_CLASSIFICATION_RULES_KIND_MUST_BUNDLE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L114
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    Bundle classification rules: - kind MUST be "bundle" for files whose

[RULE.LAYOUT.CONFIGURATION_CHANGES_MUST_AFFECT_ONLY_NEW]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L137
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.9 Session Snapshot Isolation and Persistence Model
  statement:
    Configuration changes MUST affect only new sessions.

[RULE.LAYOUT.CONFLICT_SCAN_INPUTS_NORMATIVE_CONFLICT_SCAN]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L235
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.4 Conflict Re-check Before Job Creation (Mandatory)
  statement:
    Conflict scan inputs (normative): - conflict scan MUST use planned

[RULE.LAYOUT.CONTRACT_EACH_ENTRY_MUST_CANONICAL_JSON]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L41
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    -   Contract: each entry MUST be canonical JSON and MUST be

[RULE.LAYOUT.CREATION_TIMING_PLAN_JSON_MUST_CREATED]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L69
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    Creation timing: - plan.json MUST be created/updated by compute_plan

[RULE.LAYOUT.DISCOVERY_CONFLICT_ITEMS_MUST_DETERMINISTICALLY_SORTED]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L238
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.4 Conflict Re-check Before Job Creation (Mandatory)
  statement:
    discovery. - conflict items MUST be deterministically sorted by: 1)

[RULE.LAYOUT.DISCOVERY_FINGERPRINT_MUST_EQUAL_SHA_256]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L126
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    discovery_fingerprint MUST equal: SHA-256(canonical JSON discovery set)

[RULE.LAYOUT.DISCOVERY_MUST_PRODUCE_CANONICAL_DISCOVERY_INPUT]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L109
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    Discovery MUST produce a canonical discovery input set.

[RULE.LAYOUT.DO_NOT_EXIST_UNDER_FILE_IO]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L84
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.1 Model Bootstrap When Missing
  statement:
    do not exist under the file_io root "wizards", the import plugin MUST

[RULE.LAYOUT.EACH_CREATED_JOB_MUST_HAVE_DETERMINISTIC]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L216
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.2 Idempotency Key (Mandatory)
  statement:
    Each created job MUST have a deterministic idempotency_key.

[RULE.LAYOUT.EACH_ENTRY_MUST_CONFORM_EXACTLY_CANONICAL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L163
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    Each entry MUST conform EXACTLY to the canonical job request schema

[RULE.LAYOUT.EACH_ITEM_MUST_CONTAIN_ITEM_ID]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L111
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    Each item MUST contain: - item_id - root - relative_path - kind

[RULE.LAYOUT.EFFECTIVE_MODEL_JSON_MUST_INCLUDE_ITEMS]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L32
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    effective_model.json MUST include items\[\] for the

[RULE.LAYOUT.EMPTY_PLAN_SOURCE_RELATIVE_PATH_NON]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L195
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    empty and plan.source.relative_path is non-empty, actions\[\] MUST

[RULE.LAYOUT.EXISTING_FILES_MUST_NOT_OVERWRITTEN_BOOTSTRAPPED]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L88
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.1 Model Bootstrap When Missing
  statement:
    Existing files MUST NOT be overwritten. - Bootstrapped models and config

[RULE.LAYOUT.FINALIZE_MUST_PRODUCE_BYTE_IDENTICAL_CANONICAL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L153
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10 Determinism Closure (Session Identity Tuple)
  statement:
    Finalize MUST produce byte-identical canonical job_requests.json for

[RULE.LAYOUT.FINISHES_SUCCESS_FAILED_JOBS_MUST_NOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L227
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.3 Registry Updates (SUCCESS-only)
  statement:
    finishes with SUCCESS. FAILED jobs MUST NOT update the registry.

[RULE.LAYOUT.FLOWCONFIG_UI_LAYERS_MUST_NOT_INTERPRET]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L30
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    FlowConfig. UI layers MUST NOT interpret global WizardDefinition

[RULE.LAYOUT.FORBIDDEN]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L190
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    forbidden.

[RULE.LAYOUT.GET_IMPORT_UI_STEPS_INDEX_ROUTE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L99
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.2 Step Catalog for UI Editors (Normative)
  statement:
    - The GET /import/ui/steps-index route and the GET /import/ui/steps/{step_id} route MUST be derived

[RULE.LAYOUT.IF_CONFLICTS_APPEAR_ENGINE_MUST_BLOCK]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L243
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.4 Conflict Re-check Before Job Creation (Mandatory)
  statement:
    If conflicts appear, the engine MUST block processing deterministically

[RULE.LAYOUT.IF_PRESENT_THESE_ARTIFACTS_MUST_WRITTEN]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L67
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    If present, these artifacts MUST be written atomically (temp + rename).

[RULE.LAYOUT.IF_STEP_HAS_NO_HUMAN_FRIENDLY]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L102
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.2 Step Catalog for UI Editors (Normative)
  statement:
    - If a step has no human-friendly title or description, the implementation MUST fall back

[RULE.LAYOUT.IMPORT_PLUGIN_MUST_PROVIDE_SINGLE_INTERNAL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L95
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.2 Step Catalog for UI Editors (Normative)
  statement:
    The import plugin MUST provide a single internal StepCatalog source of truth for UI step metadata.

[RULE.LAYOUT.INDEX_JSON_MUST_CONTAIN_UNIQUE_LIST]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/30_implementation_bindings.md#L258
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.7.2 Editor Storage Layout (Deterministic)
  statement:
    Index.json MUST: - contain a unique list of fingerprints - be ordered

[RULE.LAYOUT.ITEM_ID_MUST_EQUAL_ROOT_ROOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L121
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    item_id MUST equal: root:`<root>`{=html}\|path:`<relative_path>`{=html}

[RULE.LAYOUT.JOBS_REGISTRY_UPDATES_MUST_REMAIN_SAFE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L222
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.2 Idempotency Key (Mandatory)
  statement:
    jobs. - Registry updates MUST remain safe under retries.

[RULE.LAYOUT.JOB_ID_GENERATION_MUST_FOLLOW_SAME]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L172
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    -   job_id generation MUST follow the same deterministic rules as PHASE

[RULE.LAYOUT.JOB_REQUESTS_JSON_MUST_CONTAIN_CONFIG]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L185
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    job_requests.json MUST contain: - config_fingerprint (SHA-256 over

[RULE.LAYOUT.JSON_STRUCTURE_MUST_MATCH_SAME_SCHEMA]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L168
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    -   The JSON structure MUST match the same schema used by PHASE 2

[RULE.LAYOUT.MANDATORY_CRASH_RECOVERY_REQUIRED_CLI_MODE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L140
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.9 Session Snapshot Isolation and Persistence Model
  statement:
    is mandatory (crash recovery required). - CLI mode: full session

[RULE.LAYOUT.MODEL_LOAD_ABSENCE_MODELS_MUST_NOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L90
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.1 Model Bootstrap When Missing
  statement:
    model load. - Absence of models MUST NOT cause a hard failure if

[RULE.LAYOUT.MUST_NOT_MODIFY_ANY_SESSION_SNAPSHOT]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L48
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    -   MUST NOT modify any session snapshot.

[RULE.LAYOUT.MUST_PASS_FULL_MODEL_VALIDATION_BOOTSTRAP]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L89
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.1 Model Bootstrap When Missing
  statement:
    MUST pass full model validation. - Bootstrap MUST occur before first

[RULE.LAYOUT.MUST_STORED_UNDER_FLOW_CONFIG_DEFAULTS]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/30_implementation_bindings.md#L273
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.7.2 Editor Storage Layout (Deterministic)
  statement:
    MUST be stored under flow_config.defaults[step_id] as a dict.

[RULE.LAYOUT.OVER_CANONICAL_EFFECTIVE_MODEL_JSON_MODEL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: LAYOUT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L146
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10 Determinism Closure (Session Identity Tuple)
  statement:
    over canonical effective_model.json) - model_fingerprint MUST be

[RULE.LAYOUT.PERSISTED_ARTIFACTS_MUST_NOT_INCLUDE_TIMESTAMPS]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/30_implementation_bindings.md#L266
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.7.2 Editor Storage Layout (Deterministic)
  statement:
    Persisted artifacts MUST NOT include timestamps or editor metadata

[RULE.LAYOUT.PREVENTED_DUPLICATE_START_PROCESSING_CALLS_MUST]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L221
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.2 Idempotency Key (Mandatory)
  statement:
    prevented. - Duplicate start_processing calls MUST not create duplicate

[RULE.LAYOUT.PROCESSED_REGISTRY_MUST_UPDATED_ONLY_WHEN]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L226
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.3 Registry Updates (SUCCESS-only)
  statement:
    The processed registry MUST be updated only when the corresponding job

[RULE.LAYOUT.RAR_EXTENSION_MATCHING_MUST_CASE_INSENSITIVE]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L116
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    .rar - Extension matching MUST be case-insensitive.

[RULE.LAYOUT.RELATIVE_PATH_MUST_USE_SEPARATORS_CONTAIN]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L123
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.8 Deterministic Discovery (PHASE 0)
  statement:
    relative_path MUST: - use "/" separators - contain no ".", "..", empty

[RULE.LAYOUT.REQUIRED_KEYS_MUST_NOT_OMITTED]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L171
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.10A PHASE 1 Action Job Request Schema (Normative)
  statement:
    -   Required keys MUST NOT be omitted.

[RULE.LAYOUT.REQUIRED_SUBPATHS]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L16
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    Required subpaths:

[RULE.LAYOUT.RULES_DUPLICATE_JOB_CREATION_SAME_IDEMPOTENCY]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L220
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.2 Idempotency Key (Mandatory)
  statement:
    Rules: - Duplicate job creation with the same idempotency_key MUST be

[RULE.LAYOUT.SESSIONS_SESSION_ID_HTML_EFFECTIVE_MODEL]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ARTIFACT | FILE_IO
  source: docs/spec/30_implementation_bindings.md#L28
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts
  statement:
    -   sessions/`<session_id>`{=html}/effective_model.json MUST contain the

[RULE.LAYOUT.SESSIONS_SESSION_ID_HTML_PLAN_JSON]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L193
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    sessions/`<session_id>`{=html}/plan.json. - actions\[\] MUST contain one

[RULE.LAYOUT.SHARED_CANONICAL_SERIALIZE_UTILITY_MUST_EXIST]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L210
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.11.1 Canonical Serialization (Mandatory)
  statement:
    A shared canonical_serialize utility MUST exist and MUST be used by: -

[RULE.LAYOUT.START_PROCESSING_RESPONSE_CONTRACT_NORMATIVE_START]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/30_implementation_bindings.md#L198
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2)
  statement:
    start_processing response contract (normative): - start_processing MUST

[RULE.LAYOUT.STEPCATALOG_METADATA_READ_ONLY_MUST_DETERMINISTIC]
  rule_layer: LAYOUT_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/30_implementation_bindings.md#L101
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.7 Storage Model (File IO Root) and Artifacts > 10.7.2 Step Catalog for UI Editors (Normative)
  statement:
    - StepCatalog metadata is read-only and MUST be deterministic.

[RULE.LAYOUT.THIS_SEPARATION_NORMATIVE_STRUCTURE_BEHAVIOR_RENDERERS]
  rule_layer: LAYOUT_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/30_implementation_bindings.md#L275
  heading_path: AudioMason2 - Implementation Bindings Specification (Authoritative) > 10.11 Job Request Contract (PHASE 2) > 10.7.2 Editor Storage Layout (Deterministic)
  statement:
    This separation is normative: structure != behavior. Renderers MUST NOT embed

[RULE.WIRE.10_6_ENGINE_GUARDS_INVARIANTS_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L511
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.6 Engine Guards (Invariants; MUST REJECT)
  statement:
    ## 10.6 Engine Guards (Invariants; MUST REJECT)

[RULE.WIRE.ALL_EDITOR_HISTORY_ROLLBACK_BEHAVIOR_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L510
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    All editor history and rollback behavior MUST be deterministic and atomic.

[RULE.WIRE.ALL_MODES_ROUTE_LEVEL_VISIBILITY_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L62
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.1 Web server quiet mode output (CLI contract)
  statement:
    In all modes, route-level visibility MUST be provided via deterministic

[RULE.WIRE.ALL_VALIDATION_INVARIANT_FAILURES_MUST_RETURN]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L205
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.1 Error JSON (Mandatory)
  statement:
    All validation and invariant failures MUST return:

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L333
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L345
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_3]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L370
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_4]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L387
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_5]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L399
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_6]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L425
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_7]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L436
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.ANY_CONTRACT_VIOLATION_MUST_RETURN_HTTP_8]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L758
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 3. Directory Structure
  statement:
    - Any contract violation MUST return HTTP 400 with VALIDATION_ERROR (10.4.1)

[RULE.WIRE.BACKEND_MUST_EXPOSE_GET_API_ROOTS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L185
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.2 Root browsing
  statement:
    -   The backend MUST expose `GET /api/roots`.

[RULE.WIRE.BUNDLE_MUST_DETERMINISTIC_NO_TIMESTAMPED_FILENAMES]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L28
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > Debug bundle download (web)
  statement:
    The bundle MUST be deterministic: - No timestamped filenames inside the

[RULE.WIRE.CALLABLE_OPERATIONS_MUST_DISCOVERED_EXCLUSIVELY_VIA]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L554
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.19 Plugin Operation Discovery (Registry-Mediated Only)
  statement:
    Callable operations MUST be discovered exclusively via PluginRegistry.

[RULE.WIRE.CLI_MUST_EMIT_EXACTLY_ONE_SHUTDOWN]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L35
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0 Web server shutdown output (CLI contract)
  statement:
    the CLI MUST emit exactly one shutdown summary line on process exit, in

[RULE.WIRE.CONFIG_REQUIRED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L331
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - config is REQUIRED

[RULE.WIRE.CONFIG_REQUIRED_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L343
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - config is REQUIRED

[RULE.WIRE.DEBUG_MODE_HTTP_REQUESTS_THAT_RECEIVE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L170
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   In debug mode, HTTP requests that receive a non-2xx response MUST

[RULE.WIRE.DEBUG_MODE_NON_2XX_HTTP_RESPONSES]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L177
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   In debug mode, non-2xx HTTP responses MUST trigger an immediate UI

[RULE.WIRE.DEFAULTS_TEMPLATE_MUST_SUITABLE_AS_FLOWCONFIG]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L508
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - defaults_template MUST be suitable as FlowConfig.defaults[step_id] template.

[RULE.WIRE.DEFINITION_REQUIRED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L385
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - definition is REQUIRED

[RULE.WIRE.DEFINITION_REQUIRED_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L397
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - definition is REQUIRED

[RULE.WIRE.DEFINITION_REQUIRED_3]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L756
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 3. Directory Structure
  statement:
    - definition is REQUIRED

[RULE.WIRE.DESCRIPTION_MUST_ASCII_ONLY]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L504
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - description MUST be ASCII-only.

[RULE.WIRE.EACH_CALLABLE_PLUGIN_MUST_PROVIDE_MANIFEST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L587
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    Each callable plugin MUST provide a manifest with:

[RULE.WIRE.EACH_ITEM_MUST_INCLUDE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L292
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    Each item MUST include:

[RULE.WIRE.EMBEDDING_GENERAL_PURPOSE_EXPRESSION_ENGINES_FORBIDDEN]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L661
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.22 Expression Model (Sealed)
  statement:
    Embedding general-purpose expression engines is forbidden.

[RULE.WIRE.ENGINE_INVARIANT_VIOLATIONS_MUST_RETURN_INVARIANT]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L390
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Engine invariant violations MUST return INVARIANT_VIOLATION (10.4.1).

[RULE.WIRE.ENGINE_INVARIANT_VIOLATIONS_MUST_RETURN_INVARIANT_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L402
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Engine invariant violations MUST return INVARIANT_VIOLATION (10.4.1).

[RULE.WIRE.ENGINE_MUST_REJECT_ANY_WIZARDDEFINITION_FLOWCONFIG]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L513
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.6 Engine Guards (Invariants; MUST REJECT)
  statement:
    The engine MUST reject any WizardDefinition, FlowConfig, or effective workflow construction that attempts to:

[RULE.WIRE.ERRORS_FROM_CONFIG_SET_UNSET_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L102
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   Errors from config set/unset must be returned as ASCII-only

[RULE.WIRE.ERROR_CAPTURE_MUST_FAIL_SAFE_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L163
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   Error capture MUST be fail-safe and MUST NOT replace or overwrite

[RULE.WIRE.ERR_SUBMISSIONS_MUST_NOT_MUTATE_ANSWERS]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L310
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.6 SessionState (Runtime)
  statement:
    ERR submissions MUST NOT mutate answers except safe normalization.

[RULE.WIRE.EXPRESSION_RESOLUTION_MUST_SIDE_EFFECT_FREE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L659
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.22 Expression Model (Sealed)
  statement:
    - Expression resolution MUST be side-effect free

[RULE.WIRE.FIELD_DEFINITIONS_MUST_DECLARE_REQUIRED_PROPERTIES]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L247
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.3 Field Types (Baseline)
  statement:
    Field definitions MUST declare required properties appropriate for their type (engine is authoritative).

[RULE.WIRE.FILESYSTEM_SCANNING_FORBIDDEN]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L573
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.19 Plugin Operation Discovery (Registry-Mediated Only)
  statement:
    Filesystem scanning is forbidden.

[RULE.WIRE.FINALIZED_SESSIONS_MUST_IMMUTABLE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L632
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.21 Formal Session Lifecycle Extension
  statement:
    FINALIZED sessions MUST be immutable:

[RULE.WIRE.FLOWCONFIG_EDITABLE_CONFIGURATION_OPTIONAL_STEPS_DEFAULTS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L251
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.4 FlowConfig (Versioned)
  statement:
    FlowConfig is the editable configuration for optional steps and defaults. It MUST be versioned and validated.

[RULE.WIRE.FLOWCONFIG_MUST_NOT_CHANGE]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L259
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.4 FlowConfig (Versioned)
  statement:
    FlowConfig MUST NOT change:

[RULE.WIRE.FLOWCONFIG_MUST_NOT_INCLUDE_TOP_LEVEL]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L261
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.4 FlowConfig (Versioned)
  statement:
    FlowConfig MUST NOT include a top-level conflicts key.

[RULE.WIRE.HOST_E_G_WEB_INTERFACE_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L731
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 2. Responsibility Model
  statement:
    - The host (e.g. web_interface) MUST treat the plugin UI as an

[RULE.WIRE.ID_REQUIRED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L368
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - id is REQUIRED

[RULE.WIRE.ID_REQUIRED_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L423
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - id is REQUIRED

[RULE.WIRE.IF_ID_NOT_FOUND_MUST_RETURN]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L371
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - If id is not found MUST return HTTP 404 with Error code NOT_FOUND (10.4.1).

[RULE.WIRE.IF_ID_NOT_FOUND_MUST_RETURN_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L426
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - If id is not found MUST return HTTP 404 with Error code NOT_FOUND (10.4.1).

[RULE.WIRE.INTERPRETER_MUST_ENFORCE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L606
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    Interpreter MUST enforce:

[RULE.WIRE.INTERPRETER_MUST_GUARANTEE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L685
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.24 Determinism Guarantees (Extended)
  statement:
    Interpreter MUST guarantee:

[RULE.WIRE.INTERPRETER_MUST_NOT_IMPLEMENT_PARALLEL_JOB]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L617
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    Interpreter MUST NOT implement parallel job execution mechanism outside Section 5.

[RULE.WIRE.INTERPRETER_MUST_RETURN_FOLLOWING_CANONICAL_STRUCTURE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L531
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.18 Step Payload Contract (Renderer Interface)
  statement:
    The interpreter MUST return the following canonical structure:

[RULE.WIRE.INTERPRETER_SHALL]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L566
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.19 Plugin Operation Discovery (Registry-Mediated Only)
  statement:
    The interpreter SHALL:

[RULE.WIRE.INVALID_REFERENCE_MUST_RAISE_VALIDATION_ERROR]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L658
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.22 Expression Model (Sealed)
  statement:
    - Invalid reference MUST raise VALIDATION_ERROR

[RULE.WIRE.ITEMS_MUST_RETURNED_DETERMINISTIC_ORDER]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L464
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - items MUST be returned in deterministic order.

[RULE.WIRE.JOB_MUST_GENERATE_PHASE_1_ACTION]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L615
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    - job: MUST generate a PHASE 1 action job request (sessions/<session_id>/action_jobs.json) and use the existing Job subsystem; PHASE 2 processing remains governed by 10.11

[RULE.WIRE.KIND_MUST_MATCH_IMPORT_PLUGIN_S]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L467
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - kind MUST match the import plugin's runtime classification.

[RULE.WIRE.LITERAL_LINE_INTERRUPTED_MUST_NOT_PRINTED]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L43
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0 Web server shutdown output (CLI contract)
  statement:
    The literal line `Interrupted.` MUST NOT be printed.

[RULE.WIRE.LOGS_UI_MUST_EXPOSE_DOWNLOAD_DEBUG]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L149
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   The Logs UI MUST expose a "Download debug bundle" action that calls

[RULE.WIRE.LOGS_UI_MUST_INCLUDE_CLIENT_SIDE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L174
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   The Logs UI MUST include a client-side debug feed in debug mode, so

[RULE.WIRE.MODE_MUST_ONE_STAGE_INPLACE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L434
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - mode MUST be one of: stage, inplace

[RULE.WIRE.MULTI_SELECT_INDEXED_FIELDS_ENGINE_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L285
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    For multi_select_indexed fields, the engine MUST provide a concrete items[] list in the

[RULE.WIRE.MUST_EMIT_PLUGIN_LOAD_FAILED_DIAGNOSTICS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L82
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.2 Web plugin selection (CLI contract)
  statement:
    MUST emit a `plugin.load_failed` diagnostics envelope via the Core event

[RULE.WIRE.MUST_ENFORCE_DETERMINISTIC_CONFLICT_RE_CHECK]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L448
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - MUST enforce deterministic conflict re-check before job creation (10.11.4).

[RULE.WIRE.MUTATION_AFTER_FINALIZED_MUST_REJECTED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L639
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.21 Formal Session Lifecycle Extension
  statement:
    Mutation after FINALIZED MUST be rejected.

[RULE.WIRE.OPAQUE_CONSUMER_ROUTE_MUST_NOT_RE]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L732
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 2. Responsibility Model
  statement:
    opaque consumer route and MUST NOT re-implement its renderer logic.

[RULE.WIRE.OUTPUT_MUST_EXACTLY_2_LINES_NOTHING]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L48
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.1 Web server quiet mode output (CLI contract)
  statement:
    output MUST be exactly 2 lines and nothing else:

[RULE.WIRE.OUT_RANGE_INDICES_MUST_YIELD_VALIDATION]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L233
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.2 Selection Expression Grammar (Mandatory)
  statement:
    - Out-of-range indices MUST yield VALIDATION_ERROR.

[RULE.WIRE.PATH_TRAVERSAL_MUST_REJECTED_NO_SEGMENTS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L190
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.2 Root browsing
  statement:
    -   Path traversal MUST be rejected (no `..` segments) in all file

[RULE.WIRE.PINNED_MUST_FIRST_ONLY_REQUIRED_FIRST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L468
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - pinned MUST be "first" only for the required first step and "last" only for the required last step.

[RULE.WIRE.PLUGIN_MAY_CHOOSE_STORE_BOTH_CONTRACTS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L583
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    - A plugin MAY choose to store both contracts in one physical JSON file, but the registry MUST expose

[RULE.WIRE.PLUGIN_MUST_NOT_RELY_HOST_SPECIFIC]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L730
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 2. Responsibility Model
  statement:
    - The plugin MUST NOT rely on host-specific UI renderers.

[RULE.WIRE.PREVIEW_ARTIFACTS_MUST_ISOLATED_DISPOSABLE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L679
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.23 Preview Execution (Optional, Isolated)
  statement:
    Preview artifacts MUST be isolated and disposable.

[RULE.WIRE.QUIET_MODE_UVICORN_LOGGING_MUST_SILENCED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L53
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.1 Web server quiet mode output (CLI contract)
  statement:
    In quiet mode, uvicorn logging MUST be silenced (no

[RULE.WIRE.REGISTRY_METADATA_MUST_INCLUDE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L556
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.19 Plugin Operation Discovery (Registry-Mediated Only)
  statement:
    Registry metadata MUST include:

[RULE.WIRE.REJECTION_MUST_USE_INVARIANT_VIOLATION_10]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L527
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.6 Engine Guards (Invariants; MUST REJECT)
  statement:
    Rejection MUST use INVARIANT_VIOLATION (10.4.1).

[RULE.WIRE.RENDERERS_MUST_RENDER_STRICTLY_FROM_THIS]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L546
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.18 Step Payload Contract (Renderer Interface)
  statement:
    Renderers MUST render strictly from this payload.

[RULE.WIRE.RENDERERS_MUST_TREAT_FLOWMODEL_AS_SINGLE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L274
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    Renderers MUST treat FlowModel as the single source of truth for rendering.

[RULE.WIRE.REPOSITORY_MUST_ENFORCE_AUTOMATED_CHECKS_ENSURING]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L696
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.25 CI-Enforced Anti-Drift Rules
  statement:
    The repository MUST enforce automated checks ensuring:

[RULE.WIRE.REQUIRED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L282
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    - required

[RULE.WIRE.REQUIRED_FALSE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L485
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    "required": false,

[RULE.WIRE.REQUIRED_FALSE_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L491
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    "required": false,

[RULE.WIRE.RESPONSE_MUST_INCLUDE_ONLY_USER_FACING]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L186
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.2 Root browsing
  statement:
    -   The response MUST include only user-facing roots: `inbox`, `stage`,

[RULE.WIRE.RESPONSE_OBJECT_KEYS_MUST_EMITTED_DETERMINISTIC]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L503
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Response object keys MUST be emitted in deterministic order.

[RULE.WIRE.RESULTING_ID_LIST_MUST_PRESERVE_ORIGINAL]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L234
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.2 Selection Expression Grammar (Mandatory)
  statement:
    - The resulting ID list MUST preserve the original item ordering (stable selection).

[RULE.WIRE.ROOT_PATH_MODE_REQUIRED_NO_IMPLICIT]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L433
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - root, path, and mode are REQUIRED (no implicit defaults)

[RULE.WIRE.SELECT_AUTHORS_ITEMS_MUST_DERIVED_DETERMINISTICALLY]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L289
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    - select_authors items MUST be derived deterministically from discovery.json.

[RULE.WIRE.SELECT_BOOKS_ITEMS_MUST_DERIVED_DETERMINISTICALLY]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ERROR
  source: docs/spec/20_wire_contracts.md#L290
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.4 Wire Contracts (FlowConfig, FlowModel, SessionState, Errors) > 10.4.5 FlowModel (Runtime)
  statement:
    - select_books items MUST be derived deterministically from discovery.json.

[RULE.WIRE.SETTINGS_SCHEMA_FIELDS_MUST_DETERMINISTIC_ORDER]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L507
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - settings_schema.fields MUST be in deterministic order.

[RULE.WIRE.SETTINGS_SCHEMA_UI_ONLY_METADATA_READ]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L505
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - settings_schema is UI-only metadata (read-only contract) and MUST NOT include

[RULE.WIRE.STARTUP_SHUTDOWN_ACCESS_OUTPUT_UVICORN_LOG]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L54
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.1 Web server quiet mode output (CLI contract)
  statement:
    startup/shutdown/access output). Uvicorn log settings MUST map from AM

[RULE.WIRE.STEP_ID_MUST_ASCII]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L465
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - step_id MUST be ASCII.

[RULE.WIRE.STEP_ID_PATH_PARAMETER_MUST_ASCII]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L502
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - step_id path parameter MUST be ASCII and MUST match an item in steps-index.

[RULE.WIRE.TITLE_MUST_ASCII_IF_NO_HUMAN]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L466
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - title MUST be ASCII (if no human title exists, use step_id).

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L332
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_2]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L344
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_3]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L369
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_4]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L386
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_5]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L398
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_6]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L424
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_7]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L435
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.5 Import Plugin API (UI-Facing Routes)
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.UNKNOWN_REQUEST_BODY_FIELDS_MUST_REJECTED_8]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L757
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 3. Directory Structure
  statement:
    - Unknown request body fields MUST be rejected

[RULE.WIRE.WEB_INTERFACE_EXPOSES_UI_ONLY_CONFIGURATION]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L87
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    The web interface exposes a **UI-only** configuration surface. It must

[RULE.WIRE.WEB_PLUGIN_SELECTION_MUST_DETERMINISTIC_MANIFEST]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L70
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.2 Web plugin selection (CLI contract)
  statement:
    Web plugin selection MUST be deterministic and manifest-only:

[RULE.WIRE.WEB_SERVER_MUST_NOT_TAIL_WEB]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L133
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   The web server MUST NOT tail a web-specific log file as its primary

[RULE.WIRE.WEB_UI_MUST_REPLACEABLE_WITHOUT_TOUCHING]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L13
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules
  statement:
    The web UI must be replaceable without touching core logic.

[RULE.WIRE.WHEN_STARTING_WEB_MODE_CLI_MUST]
  rule_layer: WIRE_RULE
  normativity: MUST_NOT
  scope: ENDPOINT | ARTIFACT
  source: docs/spec/20_wire_contracts.md#L68
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.0.2 Web plugin selection (CLI contract)
  statement:
    When starting web mode, the CLI MUST NOT eager-load all plugins.

[RULE.WIRE.WHEN_WEB_INTERFACE_DEBUG_ENABLED_WEB]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT | CONFIG | UI
  source: docs/spec/20_wire_contracts.md#L168
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 9. Web Interface Rules > 9.1 Web Interface Configuration Surface
  statement:
    -   When `WEB_INTERFACE_DEBUG` is enabled, the web UI MUST surface

[RULE.WIRE.WIZARDDEFINITION_VALIDATE_SAVE_MUST_ACCEPT_WIZARDDEFINITION]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: ENDPOINT
  source: docs/spec/20_wire_contracts.md#L763
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > Plugin-Owned UI Architecture > 3. Directory Structure
  statement:
    - WizardDefinition validate/save MUST accept WizardDefinition v2 payloads (FlowGraph).

[RULE.WIRE.WIZARD_CALLABLE_OPERATIONS_THROUGH_WIZARD_CALLABLE]
  rule_layer: WIRE_RULE
  normativity: MUST
  scope: REGISTRY | ENDPOINT
  source: docs/spec/20_wire_contracts.md#L584
  heading_path: AudioMason2 - Wire Contracts Specification (Authoritative) > 10.20 Callable Plugin Manifest Contract
  statement:
    wizard-callable operations through wizard_callable_manifest_pointer and the interpreter MUST validate

IMPLEMENTATIONS
--------------------------------------------------------------------------------
[IMPL.SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE.V1]
  implements_route: ROUTE.SURF.10_ARCHITECTURE.AUDIOMASON2_PROJECT_SPECIFICATION_AUTHORITATIVE.V1
  providers_available (8):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.JOB_RUNNER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.ERROR_SCHEMA
    - PROV.JSON_SCHEMA
  declared_capabilities (8):
    - CAP.SCOPE_001
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_005
    - CAP.SCOPE_006
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[IMPL.SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE.V1]
  implements_route: ROUTE.SURF.20_WIRE_CONTRACTS.AUDIOMASON2_WIRE_CONTRACTS_SPECIFICATION_AUTHORITATIVE.V1
  providers_available (7):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.ERROR_SCHEMA
    - PROV.JSON_SCHEMA
  declared_capabilities (5):
    - CAP.SCOPE_002
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_008
    - CAP.SCOPE_009

[IMPL.SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE.V1]
  implements_route: ROUTE.SURF.30_IMPLEMENTATION_BINDINGS.AUDIOMASON2_IMPLEMENTATION_BINDINGS_SPECIFICATION_AUTHORITATIVE.V1
  providers_available (7):
    - PROV.FILE_IO
    - PROV.REGISTRY
    - PROV.CONFIG_RESOLVER
    - PROV.JOB_RUNNER
    - PROV.UI_EDITOR
    - PROV.ENDPOINT_GATE
    - PROV.JSON_SCHEMA
  declared_capabilities (6):
    - CAP.SCOPE_002
    - CAP.SCOPE_003
    - CAP.SCOPE_004
    - CAP.SCOPE_005
    - CAP.SCOPE_007
    - CAP.SCOPE_009
